---
phase: 01-sdk-core
plan: 05
type: execute
wave: 5
depends_on:
  - 01-01
  - 01-04
files_modified:
  - sdk/rollup.config.mjs
  - scripts/check-size.js
  - tests/e2e/sdk-injection.test.ts
  - sdk/package.json
autonomous: false
requirements:
  - SDK-04
  - SDK-07
  - TST-01
  - TST-03
  - TST-04

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "npm run build produces dist/watchbug.js in IIFE format"
    - "npm run check:size verifies the gzipped bundle is ≤45KB"
    - "Build fails if SDK exceeds 45KB gzipped"
    - "Lighthouse performance score impact is ≤2 points when SDK is injected"
    - "E2E test injects SDK into HTML with aggressive CSS and verifies widget is functional"
    - "Widget is functional even when host page has * { display: none !important } CSS"
    - "All unit tests pass via npm run test:unit"
    - "Bundle contains both en and es translations"
    - "IIFE output exposes window.Watchbug global"
  artifacts:
    - path: "sdk/rollup.config.mjs"
      provides: "Final Rollup configuration with IIFE output"
    - path: "scripts/check-size.js"
      provides: "Bundle size verification script"
      exports: ["checkBundleSize"]
    - path: "tests/e2e/sdk-injection.test.ts"
      provides: "E2E test for SDK injection and functionality"
    - path: "sdk/package.json"
      provides: "Updated scripts for build, test, size check"
  key_links:
    - from: "sdk/rollup.config.mjs"
      to: "dist/watchbug.js"
      via: "Rollup builds IIFE bundle from TypeScript sources"
      pattern: "rollup -c"
    - from: "scripts/check-size.js"
      to: "dist/watchbug.js"
      via: "Size check reads gzipped bundle and compares to 45KB limit"
      pattern: "gzip|45.*KB|check.*size"
    - from: "tests/e2e/sdk-injection.test.ts"
      to: "dist/watchbug.js"
      via: "E2E test loads the built bundle and verifies widget renders"
      pattern: "inject.*script|watchbug"

---

<objective>
Finalize the Rollup build configuration (IIFE output per D-10, D-12), implement the bundle size check script (SDK-04: ≤45KB gzipped), verify Lighthouse performance impact (SDK-07: ≤2 points), and create E2E tests that inject the SDK into a page with aggressive CSS to prove Shadow DOM isolation (TST-03). Run the full test suite to confirm all phases work together.

Purpose: This plan validates that the SDK meets its size and performance constraints and that the built bundle works end-to-end when injected via a script tag.
Output: Final build pipeline, size verification script, E2E tests, and confirmed bundle compliance.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sdk-core/01-CONTEXT.md
@.planning/phases/01-sdk-core/01-RESEARCH.md
@.planning/phases/01-sdk-core/01-01-SUMMARY.md
@.planning/phases/01-sdk-core/01-02-SUMMARY.md
@.planning/phases/01-sdk-core/01-03-SUMMARY.md
@.planning/phases/01-sdk-core/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Rollup config + bundle size check script</name>
  <read_first>
    - sdk/rollup.config.mjs (Rollup config from Plan 01 — must understand current setup)
    - sdk/package.json (package.json — must understand current scripts)
    - .planning/phases/01-sdk-core/01-CONTEXT.md (D-10: Rollup, D-12: IIFE output, D-13: CDN)
    - .planning/phases/01-sdk-core/01-RESEARCH.md (SDK-04: ≤45KB, Standard Stack: tsup/Rollup)
  </read_first>
  <files>
    sdk/rollup.config.mjs
    scripts/check-size.js
    sdk/package.json
  </files>
  <action>
    1. Update sdk/rollup.config.mjs per D-10 and D-12:
       - Ensure input: 'src/index.ts'
       - Ensure output: { file: 'dist/watchbug.js', format: 'iife', name: 'Watchbug', sourcemap: false }
       - Add terser plugin for minification (already in Plan 01, verify it's correct).
       - Add a second output for ESM: { file: 'dist/watchbug.esm.js', format: 'es' }
       - Per D-12: primary output is IIFE — single self-contained script tag.
       - Add external: [] (no external dependencies — everything bundled).

    2. Create scripts/check-size.js:
       - Read dist/watchbug.js, compress with zlib.gzipSync, check byte length.
       - Per SDK-04: bundle must be ≤45KB (46080 bytes) gzipped.
       - If size > 45KB: print error with size and limit, exit with code 1.
       - If size ≤ 45KB: print success with size, exit with code 0.
       - Also print uncompressed size for reference.
       - The script should be runnable via: `node scripts/check-size.js`

    3. Update sdk/package.json:
       - Ensure scripts.build = "rollup -c" (from Plan 01)
       - Ensure scripts.check:size = "node ../scripts/check-size.js"
       - Ensure scripts.test = "vitest run"
       - Ensure scripts.test:e2e = "vitest run --config vitest.e2e.config.ts"
       - Add scripts.test:all = "npm run test && npm run test:e2e"

    4. Build the SDK: `cd sdk && npm run build`
    5. Run size check: `cd sdk && npm run check:size`
    6. Verify the build succeeds and size check passes (or fails with clear message if over 45KB).
  </action>
  <verify>
    <automated>cd sdk && npm run build 2>&1 | grep -E "created|error" && npm run check:size 2>&1 | grep -E "size|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/rollup.config.mjs outputs IIFE format with name "Watchbug"
    - dist/watchbug.js exists after build
    - dist/watchbug.js is IIFE format (starts with "var Watchbug=" or similar IIFE wrapper)
    - scripts/check-size.js exists and checks gzipped size against 45KB limit
    - `npm run build` succeeds without errors
    - `npm run check:size` runs and reports bundle size
    - sdk/package.json has correct scripts for build, test, check:size
  </acceptance_criteria>
  <done>Rollup config finalized for IIFE output. Bundle size check script verifies ≤45KB gzipped. Build pipeline complete.</done>
</task>

<task type="auto">
  <name>Task 2: E2E tests — SDK injection with aggressive CSS + Lighthouse check</name>
  <read_first>
    - sdk/rollup.config.mjs (build config — must understand output path)
    - scripts/check-size.js (size check — must understand bundle path)
    - .planning/phases/01-sdk-core/01-CONTEXT.md (D-02: Shadow DOM isolation)
    - .planning/phases/01-sdk-core/01-RESEARCH.md (SDK-02: Shadow DOM, Pitfall 1: Closed Mode, TST-03: E2E with aggressive CSS)
    - documentation/mentorship-pack.md (INV-01: Total Widget Isolation, RNF-02: Total Isolation)
  </read_first>
  <files>
    tests/e2e/sdk-injection.test.ts
    vitest.e2e.config.ts
  </files>
  <action>
    1. Create vitest.e2e.config.ts:
       - Configure for E2E tests: include ["tests/e2e/**/*.test.ts"]
       - Set test.timeout to 10000ms
       - Set test.pool to 'forks' for isolation

    2. Create tests/e2e/sdk-injection.test.ts:
       - Per TST-03: inject SDK in HTML with aggressive CSS and verify widget is functional.
       - Test: "SDK loads asynchronously and creates window.Watchbug"
         * Create an HTML page with <script async src="dist/watchbug.js">.
         * Load in JSDOM or happy-dom environment.
         * Verify window.Watchbug exists and has init method.
       - Test: "Widget renders inside Shadow DOM — immune to host CSS"
         * Create HTML with aggressive CSS: * { display: none !important; }
         * Inject the SDK script.
         * Call window.Watchbug.init({ key: 'test' }).
         * Query for the watchbug-widget custom element.
         * Verify the widget's shadow root exists and has visible content.
         * Per INV-01: host CSS cannot break widget styling.
       - Test: "Widget has two buttons: Report Bug and Send Feedback"
         * After init, query shadow DOM for buttons with text "Report Bug" and "Send Feedback".
       - Test: "Clicking Report Bug shows full-screen overlay"
         * Simulate click on Report Bug button.
         * Verify overlay is visible (not hidden).
       - Test: "Overlay has canvas element for annotations"
         * After overlay shown, verify canvas element exists in shadow DOM.

    3. Run E2E tests: `cd sdk && npx vitest run --config vitest.e2e.config.ts`

    4. For Lighthouse check (SDK-07):
       - Create a simple HTML test page that loads the SDK.
       - Run Lighthouse via CLI: `lighthouse <url> --only-categories=performance --output=json`
       - Check that performance score impact is ≤2 points.
       - If Lighthouse is not available in CI, document the manual verification steps.
       - Per SDK-07: Lighthouse performance score impact ≤2 points.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run --config vitest.e2e.config.ts 2>&1 | grep -E "Tests|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - vitest.e2e.config.ts exists with correct E2E configuration
    - tests/e2e/sdk-injection.test.ts exists with at least 4 tests
    - E2E test verifies SDK loads and creates window.Watchbug
    - E2E test verifies widget renders inside Shadow DOM
    - E2E test verifies widget has two buttons (Report Bug / Send Feedback)
    - E2E test verifies clicking button shows overlay
    - E2E test passes with aggressive CSS (* { display: none !important }) proving isolation per INV-01
    - All E2E tests pass
  </acceptance_criteria>
  <done>E2E tests verify SDK injection, Shadow DOM isolation, and widget functionality even with aggressive host CSS. Lighthouse check documented.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Full test suite + bundle verification + final review</name>
  <read_first>
    - sdk/package.json (must understand all scripts)
    - sdk/rollup.config.mjs (build config)
    - scripts/check-size.js (size check)
    - tests/e2e/sdk-injection.test.ts (E2E tests)
  </read_first>
  <what-built>Complete Watchbug SDK with Shadow DOM widget, capture engine, canvas editor, transport layer, i18n, build pipeline, and E2E tests</what-built>
  <how-to-verify>
    1. Run full test suite: `cd sdk && npm run test:all`
    2. Build SDK: `cd sdk && npm run build`
    3. Check bundle size: `cd sdk && npm run check:size`
    4. Verify bundle is ≤45KB gzipped
    5. Open tests/e2e/sdk-injection.test.ts and confirm all E2E tests pass
    6. Verify dist/watchbug.js exists and is IIFE format
    7. Verify dist/watchbug.js contains both 'Report Bug' and 'Send Feedback' strings (i18n bundled)
  </how-to-verify>
  <acceptance_criteria>
    - `npm run test:all` exits 0 with all tests passing
    - `npm run build` produces dist/watchbug.js
    - `npm run check:size` reports bundle ≤45KB gzipped
    - dist/watchbug.js contains window.Watchbug global
    - dist/watchbug.js contains both en and es translations
  </acceptance_criteria>
  <resume-signal>Type "approved" to confirm all tests pass and bundle meets size constraints, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Pipeline → Distribution | Built bundle must be minified and size-constrained |
| Host Page → SDK Bundle | Bundle must be self-contained, no external requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Denial of Service | Bundle size | high | mitigate | check-size.js enforces ≤45KB gzipped — build fails if exceeded |
| T-05-02 | Tampering | Host CSS | medium | mitigate | E2E tests verify widget works with aggressive host CSS per INV-01 |
| T-05-03 | Information Disclosure | Bundle contents | low | accept | Bundle contains only SDK code and i18n strings — no secrets |
</threat_model>

<verification>
1. Full test suite passes: `cd sdk && npm run test:all` exits 0
2. Build succeeds: `cd sdk && npm run build` produces dist/watchbug.js
3. Size check passes: `cd sdk && npm run check:size` reports ≤45KB
4. E2E tests pass: widget renders inside Shadow DOM even with aggressive CSS
5. Bundle is IIFE format: dist/watchbug.js starts with IIFE wrapper pattern
6. Bundle contains i18n: grep for "Report Bug" and "Reportar Error" in dist/watchbug.js returns matches
</verification>

<success_criteria>
- SDK bundle ≤45KB gzipped
- Build produces IIFE format with window.Watchbug global
- E2E tests verify Shadow DOM isolation
- Lighthouse impact ≤2 points (or documented manual verification)
- All tests pass (unit + E2E)
- Bundle contains both en and es translations
</success_criteria>

<output>
Create `.planning/phases/01-sdk-core/01-05-SUMMARY.md` when done
</output>
