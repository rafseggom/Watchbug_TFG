---
phase: 01-sdk-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - sdk/src/capture/screenshot.ts
  - sdk/src/capture/metadata.ts
  - sdk/src/capture/console.ts
  - sdk/src/capture/batcher.ts
  - sdk/src/index.ts
  - tests/unit/screenshot.test.ts
  - tests/unit/metadata.test.ts
  - tests/unit/console.test.ts
  - tests/unit/batcher.test.ts
autonomous: true
requirements:
  - CAP-01
  - CAP-02
  - CAP-03
  - CAP-05
  - CAP-06
  - D-04
  - D-14
  - D-15
  - D-16
  - D-17

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Screenshot capture produces a Blob or DataURL of the current viewport at max 1280px width"
    - "Screenshot capture completes within 500ms or returns a timeout error"
    - "Metadata includes URL, User-Agent, screen width/height, viewport width/height, and timestamp"
    - "Console.log, console.warn, console.error, console.info are all intercepted"
    - "Console messages containing patterns matching API keys, tokens, or passwords are redacted before storage"
    - "Console messages are truncated to 500 characters maximum"
    - "Ring buffer stores up to configurable maxEntries (default 50) entries"
    - "Oldest entries are evicted when buffer reaches capacity"
    - "Event batcher queues reports and flushes on interval or when batch size is reached"
    - "Failed flush re-queues the batch for later retry"
    - "window.onerror handler stores uncaught error info in the buffer per D-04"
    - "All four console methods (log, warn, error, info) are intercepted per D-16"
    - "Console logs are always included in report payload per D-17 — no developer toggle"
    - "Incident type (Bug vs Feedback) is distinguished in the capture payload per CAP-06"
  artifacts:
    - path: "sdk/src/capture/screenshot.ts"
      provides: "Viewport screenshot capture via Canvas API"
      exports: ["captureScreenshot"]
    - path: "sdk/src/capture/metadata.ts"
      provides: "Environment metadata collection"
      exports: ["collectMetadata"]
    - path: "sdk/src/capture/console.ts"
      provides: "Console interception with secret redaction"
      exports: ["startConsoleCapture", "getConsoleBuffer"]
    - path: "sdk/src/capture/batcher.ts"
      provides: "Event batching with configurable flush"
      exports: ["EventBatcher"]
    - path: "sdk/src/index.ts"
      provides: "Updated entry point wiring capture modules"
  key_links:
    - from: "sdk/src/capture/console.ts"
      to: "sdk/src/index.ts"
      via: "startConsoleCapture called during init() to begin interception"
      pattern: "startConsoleCapture"
    - from: "sdk/src/capture/screenshot.ts"
      to: "sdk/src/capture/metadata.ts"
      via: "Screenshot and metadata collected together for report payload"
      pattern: "captureScreenshot|collectMetadata"
    - from: "sdk/src/capture/batcher.ts"
      to: "sdk/src/index.ts"
      via: "EventBatcher instantiated during init() for report delivery"
      pattern: "new EventBatcher|EventBatcher"

---

<objective>
Implement the capture engine: screenshot capture via Canvas API (viewport-only, max 1280px, 500ms timeout), environment metadata collection, console log interception with secret redaction (D-14, D-16), ring buffer with configurable size (D-15), window.onerror auto-capture (D-04), and event batching with graceful degradation. Wire all capture modules into the SDK entry point so that init() starts capturing.

Purpose: After this plan, the SDK captures all the data needed for a bug report — screenshot, metadata, console logs, and errors — and queues them for transport.
Output: Four capture modules (screenshot, metadata, console, batcher) integrated into the SDK entry point, with comprehensive unit tests.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-sdk-core/01-CONTEXT.md
@.planning/phases/01-sdk-core/01-RESEARCH.md
@.planning/phases/01-sdk-core/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Screenshot capture via Canvas API + metadata collection</name>
  <read_first>
    - sdk/src/index.ts (entry point from Plan 01 — must understand init flow and config shape)
    - .planning/phases/01-sdk-core/01-CONTEXT.md (D-01: init config, D-07: submit flow)
    - .planning/phases/01-sdk-core/01-RESEARCH.md (Pattern 2: Canvas, Pitfall 2: Memory Explosion, CAP-01/CAP-02 specs)
    - documentation/mentorship-pack.md (SEC-01: auto-sanitization)
  </read_first>
  <files>
    sdk/src/capture/screenshot.ts
    sdk/src/capture/metadata.ts
    tests/unit/screenshot.test.ts
    tests/unit/metadata.test.ts
  </files>
  <action>
    1. Create sdk/src/capture/screenshot.ts:
       - Export async function captureScreenshot(options?: { maxWidth?: number; timeout?: number }): Promise<{ dataUrl: string; width: number; height: number } | null>
       - Default maxWidth = 1280 per CAP-01, default timeout = 500ms per CAP-01.
       - Implementation per RESEARCH Pattern 2 and Pitfall 2:
         * Use html-canvas approach: create an offscreen canvas, draw the viewport onto it.
         * Per CAP-01: viewport-only capture — use window.innerWidth and window.innerHeight for dimensions.
         * If viewport width > maxWidth, scale down proportionally.
         * Wrap in a Promise with setTimeout for the timeout. If capture takes > timeout, resolve with null.
         * Use canvas.toBlob('image/png') for memory efficiency per RESEARCH State of the Art.
         * Convert blob to dataUrl via FileReader if needed, or use canvas.toDataURL('image/png').
       - Handle tainted canvas errors (SecurityError) by catching and returning null.
       - Per CAP-01: max 1280px width, 500ms timeout.

    2. Create sdk/src/capture/metadata.ts:
       - Export function collectMetadata(): IncidentMetadata
       - IncidentMetadata type: { url: string; userAgent: string; screenWidth: number; screenHeight: number; viewportWidth: number; viewportHeight: number; timestamp: string; language: string }
       - Per CAP-02: URL from window.location.href, userAgent from navigator.userAgent, screen from window.screen.width/height, viewport from window.innerWidth/innerHeight, timestamp as ISO string.
       - Per CAP-02: use standard browser APIs — no external dependencies.

    3. Create tests/unit/screenshot.test.ts:
       - Mock HTMLCanvasElement and CanvasRenderingContext2D
       - Test: "captureScreenshot returns data URL with dimensions"
       - Test: "captureScreenshot respects maxWidth parameter"
       - Test: "captureScreenshot returns null on timeout"
       - Test: "captureScreenshot returns null on SecurityError (tainted canvas)"
       - Test: "captureScreenshot uses viewport dimensions (innerWidth/innerHeight)"

    4. Create tests/unit/metadata.test.ts:
       - Mock window.location, navigator.userAgent, screen, window.innerWidth/innerHeight
       - Test: "collectMetadata returns URL from window.location.href"
       - Test: "collectMetadata returns userAgent from navigator.userAgent"
       - Test: "collectMetadata includes screen and viewport dimensions"
       - Test: "collectMetadata timestamp is ISO format"
       - Test: "collectMetadata includes language from navigator.language"

    5. Update sdk/src/index.ts to import and expose captureScreenshot and collectMetadata on the WatchbugAPI (or as internal methods used by the submit flow in Plan 04).
  </action>
  <verify>
    <automated>cd sdk && npx vitest run tests/unit/screenshot.test.ts tests/unit/metadata.test.ts 2>&1 | grep -E "Tests|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/capture/screenshot.ts exports captureScreenshot async function
    - captureScreenshot uses Canvas API (not html2canvas) per RESEARCH recommendation
    - captureScreenshot default maxWidth is 1280 per CAP-01
    - captureScreenshot default timeout is 500ms per CAP-01
    - captureScreenshot returns null on SecurityError (tainted canvas)
    - sdk/src/capture/metadata.ts exports collectMetadata function
    - collectMetadata returns object with url, userAgent, screenWidth, screenHeight, viewportWidth, viewportHeight, timestamp, language
    - All screenshot.test.ts and metadata.test.ts tests pass
  </acceptance_criteria>
  <done>Screenshot capture works via Canvas API with viewport capping and timeout. Metadata collection gathers URL, UA, screen, viewport, timestamp. Both modules have passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Console log interception with secret redaction + ring buffer</name>
  <read_first>
    - sdk/src/index.ts (entry point — init config includes bufferSize per D-15)
    - .planning/phases/01-sdk-core/01-CONTEXT.md (D-14: auto-redact secrets, D-15: configurable ring buffer, D-16: intercept 4 methods, D-17: always included)
    - .planning/phases/01-sdk-core/01-RESEARCH.md (Pitfall 4: Console Secrets, Code Example: Console Interception)
  </read_first>
  <files>
    sdk/src/capture/console.ts
    sdk/src/index.ts
    tests/unit/console.test.ts
  </files>
  <action>
    1. Create sdk/src/capture/console.ts:
       - Define ConsoleEntry type: { level: 'log' | 'warn' | 'error' | 'info'; message: string; timestamp: string }
       - Define SECRET_PATTERNS array per D-14 and RESEARCH Code Example:
         * /password['":\s=]+[^\s'"]+/gi
         * /token['":\s=]+[^\s'"]+/gi
         * /api[_-]?key['":\s=]+[^\s'"]+/gi
         * /secret['":\s=]+[^\s'"]+/gi
         * /authorization['":\s=]+[^\s'"]+/gi
         * /Bearer\s+[^\s'"]+/gi
         * JWT pattern: /eyJ[A-Za-z0-9-_=]+?\.[A-Za-z0-9-_=]+\.?[A-Za-z0-9-_.+/=]*/g
       - Export function redactSecrets(message: string): string — applies SECRET_PATTERNS and truncates to 500 chars per Pitfall 4.
       - Export function createConsoleBuffer(maxEntries: number = 50 per D-15): ConsoleBuffer
       - ConsoleBuffer interface: { entries: ConsoleEntry[], add(entry: ConsoleEntry): void, getAll(): ConsoleEntry[], clear(): void }
       - The add() method per D-15: when entries.length >= maxEntries, shift() the oldest entry before push(). This is the ring buffer behavior.
       - Export function startConsoleCapture(buffer: ConsoleBuffer): () => void (returns stop function)
       - startConsoleCapture per D-16: intercepts console.log, console.warn, console.error, console.info by saving originals and replacing with wrappers.
       - Each wrapper: applies redactSecrets to string args, creates ConsoleEntry, adds to buffer, then calls original console method.
       - Per D-17: console logs are always included — no developer toggle to disable capture.
       - The stop function restores original console methods.

    2. Update sdk/src/index.ts:
       - In createWatchbug(), during init(): create a console buffer with bufferSize from config (default 50 per D-15).
       - Call startConsoleCapture(buffer) to begin interception.
       - Store the stop function for cleanup.
       - Expose getConsoleLogs() that returns buffer.getAll().
       - Per D-04: add window.onerror handler that creates a ConsoleEntry with level 'error' and the error message, adds to buffer.
       - The onerror handler signature: (message, source, lineno, colno, error) => void.

    3. Create tests/unit/console.test.ts:
       - Test: "redactSecrets replaces API key patterns with [REDACTED]"
       - Test: "redactSecrets replaces JWT tokens with [REDACTED]"
       - Test: "redactSecrets replaces password patterns with [REDACTED]"
       - Test: "redactSecrets truncates messages to 500 characters"
       - Test: "createConsoleBuffer respects maxEntries (ring buffer behavior)"
       - Test: "createConsoleBuffer evicts oldest entry when full"
       - Test: "startConsoleCapture intercepts console.log"
       - Test: "startConsoleCapture intercepts console.warn, console.error, console.info"
       - Test: "stop function restores original console methods"
       - Test: "window.onerror adds error entry to buffer"

    4. Run `cd sdk && npx vitest run` to verify all tests pass.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run tests/unit/console.test.ts 2>&1 | grep -E "Tests|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/capture/console.ts exports startConsoleCapture, createConsoleBuffer, redactSecrets
    - SECRET_PATTERNS array contains at least 7 regex patterns (password, token, api_key, secret, authorization, Bearer, JWT)
    - redactSecrets("password=abc123secret") returns "[REDACTED]" (or similar redacted output)
    - redactSecrets truncates to 500 characters
    - Ring buffer evicts oldest entry when maxEntries reached (default 50 per D-15)
    - startConsoleCapture intercepts all 4 console methods per D-16
    - stop() restores original console methods
    - window.onerror handler stores errors in buffer per D-04
    - All console.test.ts tests pass
  </acceptance_criteria>
  <done>Console log interception captures all four methods with secret redaction. Ring buffer stores configurable number of entries with eviction. window.onerror captures uncaught errors.</done>
</task>

<task type="auto">
  <name>Task 3: Event batching with configurable flush + graceful degradation</name>
  <read_first>
    - sdk/src/index.ts (entry point — must understand how batcher integrates)
    - sdk/src/capture/screenshot.ts (screenshot capture — batcher will queue reports containing screenshots)
    - sdk/src/capture/metadata.ts (metadata collection)
    - .planning/phases/01-sdk-core/01-CONTEXT.md (D-07: submit flow, D-08: network failure handling)
    - .planning/phases/01-sdk-core/01-RESEARCH.md (Pattern 3: Event Batching, CAP-05 spec)
  </read_first>
  <files>
    sdk/src/capture/batcher.ts
    sdk/src/index.ts
    tests/unit/batcher.test.ts
  </files>
  <action>
    1. Create sdk/src/capture/batcher.ts:
       - Define ReportPayload type: { type: 'bug' | 'feedback'; screenshot: string; metadata: Record<string, any>; consoleLogs: ConsoleEntry[]; errors: string[]; notes?: string }
       - Per CAP-06: type field distinguishes Bug from Feedback reports.
       - Export class EventBatcher:
         * Constructor: (flushFn: (batch: ReportPayload[]) => Promise<void>, options?: { batchSize?: number; flushIntervalMs?: number })
         * Default batchSize = 5, default flushIntervalMs = 3000 per RESEARCH Pattern 3.
         * Private queue: ReportPayload[] = []
         * Private timer: ReturnType<typeof setInterval> | null = null
         * enqueue(report: ReportPayload): void — adds to queue, if queue.length >= batchSize, calls flush().
         * async flush(): Promise<void> — if queue empty, return. Splice batchSize items from queue. Call flushFn(batch). On error, unshift the batch back to queue for retry per D-08.
         * start(): void — sets up setInterval calling flush() at flushIntervalMs.
         * stop(): void — clears interval.
         * getQueueLength(): number — returns queue.length.

    2. Update sdk/src/index.ts:
       - In createWatchbug(), during init(): create an EventBatcher with a flushFn that will be implemented in Plan 04 (transport sender). For now, use a placeholder that logs to console.
       - Store the batcher instance on the WatchbugAPI.
       - Expose a submitReport(report: ReportPayload) method that calls batcher.enqueue(report).
       - Per D-07: submit flow goes through the batcher.

    3. Create tests/unit/batcher.test.ts:
       - Test: "EventBatcher enqueues reports"
       - Test: "EventBatcher flushes when batch size reached"
       - Test: "EventBatcher flushes on interval"
       - Test: "Failed flush re-queues the batch"
       - Test: "flush() does nothing when queue is empty"
       - Test: "stop() clears the interval"
       - Test: "getQueueLength() returns current queue size"

    4. Run `cd sdk && npx vitest run` to verify all tests pass.
  </action>
  <verify>
    <automated>cd sdk && npx vitest run tests/unit/batcher.test.ts 2>&1 | grep -E "Tests|PASS|FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sdk/src/capture/batcher.ts exports EventBatcher class
    - EventBatcher has enqueue, flush, start, stop, getQueueLength methods
    - Default batchSize is 5
    - Default flushIntervalMs is 3000
    - flush() re-queues batch on error (graceful degradation per D-08)
    - ReportPayload type has type field with 'bug' | 'feedback' per CAP-06
    - All batcher.test.ts tests pass
  </acceptance_criteria>
  <done>Event batching queues reports, flushes on size threshold or interval, and gracefully re-queues on failure. All capture modules are wired into the SDK entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDK → Browser Console | SDK intercepts console methods; must not break host app debugging |
| SDK → Canvas | Screenshot capture reads viewport pixels; must not capture beyond viewport |
| Console Buffer → Transport | Redacted logs sent to backend; must not leak secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Console interception | high | mitigate | Regex-based secret redaction (D-14) applied to all captured messages before storage |
| T-02-02 | Information Disclosure | Screenshot capture | medium | mitigate | Auto-sanitize password fields, credit cards, data-watchbug-sensitive elements before capture (CAP-04, implemented in Plan 03) |
| T-02-03 | Denial of Service | Ring buffer | low | mitigate | Configurable maxEntries (default 50) prevents unbounded memory growth (D-15) |
| T-02-04 | Information Disclosure | Console capture scope | medium | mitigate | Only log/warn/error/info intercepted — no dir/table/object capture; messages truncated to 500 chars |
</threat_model>

<verification>
1. All unit tests pass: `cd sdk && npx vitest run` exits 0
2. Console redaction works: redactSecrets("password=abc123") contains "[REDACTED]"
3. Ring buffer evicts oldest: buffer with maxEntries=2, add 3 entries, getAll() returns 2 entries (newest)
4. Batcher flushes on size: enqueue batchSize+1 items, flushFn called once
5. Batcher re-queues on error: flushFn throws, batch re-appears in queue
</verification>

<success_criteria>
- Screenshot capture produces viewport-only PNG at max 1280px width within 500ms
- Metadata includes URL, UA, screen, viewport, timestamp, language
- All four console methods intercepted with secret redaction
- Ring buffer stores configurable entries with eviction
- Event batcher queues and flushes reports with retry on failure
- window.onerror captures uncaught errors
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/01-sdk-core/01-02-SUMMARY.md` when done
</output>
