---
phase: 03-admin-panel
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - panel/src/views/list.ts
  - panel/src/components/badges.ts
  - panel/src/components/skeleton.ts
  - panel/src/utils/format.ts
  - panel/src/utils/sanitize.ts
  - panel/src/styles/components.css
  - panel/src/styles/responsive.css
  - panel/src/views/list.test.ts
autonomous: true
requirements:
  - PAN-03
  - PAN-04
  - PAN-06
  - PAN-07
estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Incident list shows paginated table with columns type badge (Bug/Feedback), status pill (Pending/In Progress/Resolved), date localized via Intl.DateTimeFormat, preview 40px placeholder icon when has_screenshot true — row click navigates to #/incidents/:id"
    - "Filter bar has Type dropdown (All/Bug/Feedback) and Status dropdown (All/Pending/In Progress/Resolved); selecting resets to page 1, updates hash query #/incidents?type=Bug&status=Pending so links are shareable and drive ?type&status on backend (comma-separated, TitleCase)"
    - "Footer shows Page page of pages (Total total) with Prev disabled on page 1 and Next disabled on last page; page size fixed 20 (backend default) not selectable; error 422 on invalid filter and 429 handling"
    - "Loading shows 5 skeleton shimmer rows, empty shows No incidents yet or No results for this filter with Clear Filters localized, error shows inline + Retry re-fetches with credentials:include; all rendering via textContent never innerHTML"
    - "Panel works on desktop and tablet: table is horizontally scrollable on narrow, thumbnails hide below 768, badges remain visible; detail stacks below 900 already covered but list compaction at 768 is verified"
  artifacts:
    - path: panel/src/views/list.ts
      provides: Paginated filtered incident listing view with hash-synced filters and pagination
    - path: panel/src/components/badges.ts
      provides: Type/status badge renderers using textContent
    - path: panel/src/components/skeleton.ts
      provides: 5-row skeleton shimmer component
    - path: panel/src/utils/format.ts
      provides: Localized date formatter via Intl.DateTimeFormat per RNF-03
    - path: panel/src/utils/sanitize.ts
      provides: Client href scheme guard (javascript: rejection) complementing textContent
  key_links:
    - from: panel/src/views/list.ts
      to: backend/app/routers/incidents.py GET /api/incidents
      via: "apiFetch GET /api/incidents?page&size&type&status with credentials:include -> {items,total,page,size,pages} -> table rows"
    - from: panel/src/views/list.ts
      to: panel/src/utils/hash.ts
      via: "filter change builds hash #/incidents?type=Bug&status=Pending via buildHash and triggers hashchange -> re-fetch with new query params"
    - from: panel/src/views/list.ts
      to: panel/src/components/badges.ts
      via: "renderTypeBadge/Badge called per row with textContent and class type-Bug etc."
---

<objective>
Incident listing with filtering, pagination, and responsive states. Delivers table with badges/date/preview, filter bar synced to hash URL driving backend query, Prev/Next pagination, skeleton/empty/error states with textContent safety, and CSS responsiveness for tablet.

Purpose: Satisfy PAN-03/04/06/07 listing excellence — users must browse, filter shareably, paginate, and handle empty/error gracefully without XSS.
Output: #/incidents renders populated table when incidents exist, empty illustration with Clear Filters when total=0, skeleton while loading, and responsive compaction below 768px.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-panel/03-CONTEXT.md
@.planning/phases/03-admin-panel/03-RESEARCH.md
@.planning/phases/03-admin-panel/03-PATTERNS.md
@.planning/phases/03-admin-panel/03-VALIDATION.md
@.planning/phases/03-admin-panel/03-01-SUMMARY.md
@backend/app/utils/pagination.py
@backend/app/schemas/incident.py
</context>

<tasks>

<task type="auto">
  <name>List table + badges + date + preview + pagination wired to API</name>
  <files>panel/src/views/list.ts, panel/src/components/badges.ts, panel/src/utils/format.ts, panel/src/utils/sanitize.ts</files>
  <read_first>03-CONTEXT.md D-09..D-12, 03-RESEARCH.md Patterns 5-6 + Pitfalls 3-5, 03-PATTERNS.md Pagination/Filtering + XSS Shared Patterns, backend/app/utils/pagination.py:1-60, backend/app/schemas/incident.py:IncidentOut, panel/src/api.ts, panel/src/router.ts, panel/src/i18n/index.ts</read_first>
  <acceptance_criteria>GET /api/incidents?page&size&type&status with credentials:include renders table: header sticky, columns Type badge (Bug #d73a4a else blue), Status pill yellow/blue/green, Date via Intl.DateTimeFormat(lang), Preview placeholder when has_screenshot true else dash; row click -> #/incidents/:id; pagination footer Page X of N (Total T) with Prev disabled page===1, Next disabled page===pages, fetching page via ?page param; uses items/total/page/size/pages contract [VERIFIED: incident.py pagination] with size fixed 20; no expectation of screenshot on list (has_screenshot boolean only) per Pitfall 3</acceptance_criteria>
  <action>Per D-09 (table + type/status badges reversible), D-10 (Prev/Next + dropdowns synced to hash reversible), D-12 (fixed 20 + CSS-responsive reversible), PAN-03/04/06 and RESEARCH Patterns 5-6:
- Create panel/src/components/badges.ts with export function renderTypeBadge(type:string):HTMLElement that creates span.badge with className `badge type-${type}` and textContent = type, background per spec (Bug red #d73a4a, Feedback #0366d6) via CSS classes not inline? Use class + textContent; similarly export renderStatusBadge(status:string) with class `pill status-${slug}` where slug is status.replace(" ","-").toLowerCase() and textContent = status, colors via CSS variables pending/ progress/ resolved.
- Create panel/src/utils/format.ts with export function formatDate(iso:string, lang:string):string that does new Intl.DateTimeFormat(lang,{dateStyle:"medium", timeStyle:"short"}).format(new Date(iso)); fallback to iso if invalid date; handle es/en per RNF-03 D-08 getLang().
- Create panel/src/utils/sanitize.ts with export function isSafeHref(url:string):boolean that returns false if javascript: scheme (case-insensitive) or data:text/html, true otherwise; used for detail URL later but add now for shared pattern. No other hand-rolling.
- Create panel/src/views/list.ts with export async function renderList(root:HTMLElement, query:Record&lt;string,string&gt;) that: clears root, renders header via renderHeader, renders filter bar with two selects (Type: All, Bug, Feedback values TitleCase; Status: All, Pending, In Progress, Resolved TitleCase) setting selected from location.hash query values (Object.fromEntries URLSearchParams), renders table wrapper overflow-x:auto with &lt;table&gt; header sticky (4 columns: Type, Status, Date, Preview), tbody placeholder for rows; renders footer with Page info span + Prev/Next buttons; implements fetchAndRender(page) which reads current filter selects and hash query merged, computes size=20 fixed, builds query string ?page=&amp;size=&amp;type=&amp;status=&amp; (omit All), calls await apiFetch(`/api/incidents?page=${page}&amp;size=20&amp;type=${t}&amp;status=${s}`) with credentials:include; on 200 json {items,total,page,size,pages} renders rows via for each item: create tr with 4 td via createElement + textContent + badge helpers, add click listener -> location.hash=`#/incidents/${item.id}`, append; updates footer page info textContent via t() keys and disables buttons; on 401 delegated to apiFetch refresh then retry else header guard redirects; on 422 show inline error via textContent and toast; on 429 show Retry-After if present; on network error show error div with textContent and Retry button that re-calls fetchAndRender. Handle pagination: Prev click decrements page and updates hash page via navigate(buildHash(...)), Next increments; filter select change resets page=1 and updates hash with new type/status via buildHash then triggers render. Preserve filter state in hash so manual hash navigation works bookmarkable (shareable) per D-10.
- Ensure table scrollable wrapper and badge/date/preview via textContent only per PAN-07; has_screenshot ? "◉" : "—" textContent for preview column, never img decode on list per Pitfall 3. Type/Status values are TitleCase exactly matching backend ALLOWED_* per Pitfall 4-5 mirror.
- Wire router: list route calls renderList with query object from parseHash. Ensure list fetches on mount and on hash change (filter change already navigates hash which re-triggers onRoute).
</action>
  <verify>
    <automated>vitest run --config panel/vitest.config.ts panel/src/views/list.test.ts -t "table|pagination|filter" --reporter verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Paginated table renders badges/date/preview from has_screenshot, row click to detail, Prev/Next disabled states, filter hash sync shareable driving ?type&status, fixed size 20 contract, all via textContent.</done>
</task>

<task type="auto">
  <name>Loading/empty/error states + responsive table CSS</name>
  <files>panel/src/components/skeleton.ts, panel/src/views/list.ts, panel/src/styles/components.css, panel/src/styles/responsive.css, panel/src/views/list.test.ts</files>
  <read_first>03-CONTEXT.md D-11 (skeleton+empty+escaped reversible), D-12 (breakpoints), 03-RESEARCH.md Pitfall 6 + Pattern 8, panel/src/components/skeleton.ts placeholder</read_first>
  <acceptance_criteria>Loading state shows 5 skeleton shimmer rows; empty total=0 shows illustration/icon + localized No incidents yet / No results for this filter (when filters active) with Clear Filters button resetting hash to #/incidents; error network/401 shows inline error + Retry button re-fetches with credentials:include; responsive at &lt;768 list hides thumbnail column or collapses to stacked cards, keeps badges visible, horizontal scroll fallback; all empty/error messages via textContent</acceptance_criteria>
  <action>Per D-11 (skeleton rows + empty illustration + escaped rendering reversible) and D-12 (responsive) and RESEARCH Pattern 8:
- Create panel/src/components/skeleton.ts with export function renderSkeletonRows(n=5):DocumentFragment that creates 5 tr.placeholder with 4 td each containing div.skeleton (CSS shimmer) via textContent-less divs; used while awaiting fetch.
- Enhance panel/src/views/list.ts to handle states: before fetchAndRender show skeleton fragment (clear tbody and append skeleton rows); after fetch if items.length===0 and total===0 show empty illustration: create div.empty with icon div.textContent "📭" and p.textContent=t("list.emptyNoIncidents") or t("list.emptyNoResults") when filters active (type/status !== All), and button Clear Filters textContent=t("list.clearFilters") click -> location.hash="#/incidents" (reset); if error (throw or !ok) show div.error with p.textContent via t("errors.network") or backend detail, plus button Retry textContent=t("list.retry") click -> fetchAndRender(currentPage). Ensure loading→empty/error branching covers 5 skeleton rows → illustration → error retry per D-11. All via createElement+textContent.
- Update panel/src/styles/components.css with .skeleton {background: linear-gradient(90deg, #eee 25%, #f5f5f5 37%, #eee 63%); animation: shimmer 1.4s infinite; height:16px; border-radius:4px;}, @keyframes shimmer, .empty, .error styles.
- Update panel/src/styles/responsive.css with @media (max-width:768px) { table th:nth-child(4), td:nth-child(4) {display:none} /* hide thumbnail */; table {font-size:14px} ; .filter-bar {flex-wrap:wrap} } to satisfy PAN-06 tablet compaction; verify table wrapper overflow-x:auto already in components.css. Keep 900px for detail but list uses 768 as locked D-12.
- Create panel/src/views/list.test.ts covering: test "table renders items with badges and pagination" mocking apiFetch GET success with {items:[{id,type:"Bug",status:"Pending",created_at:has_screenshot:true}], total:1, page:1, size:20, pages:1} asserts table rows length, badge textContent, status pill, date calls formatDate, preview ◉, footer Page 1 of 1; test "filter sync updates hash" selecting Type Bug asserts location.hash contains type=Bug and re-fetch with ?type=Bug; test "loading 5 skeleton rows" before fetch resolves shows 5 .skeleton; test "empty with Clear Filters" total 0 shows empty illustration with button; test "error retry" network failure shows Retry button and second click re-fetches; test "responsive css class" asserts table has overflow wrapper. Use jsdom.
- Verify no innerHTML in list.ts/skeleton.ts via grep.
</action>
  <verify>
    <automated>vitest run --config panel/vitest.config.ts panel/src/views/list.test.ts --reporter verbose 2>&1 | tail -40; grep -rn "innerHTML" panel/src --quiet && echo "innerHTML found FAIL" || echo "no innerHTML ok"</automated>
  </verify>
  <done>List handles loading 5 skeleton rows, empty with Clear Filters localized, error with Retry re-fetch credentials:include, hash filter sync shareable, responsive 768 hides thumbnail, all via textContent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser -> GET /api/incidents paginated | Authenticated but filter params type/status are user-controlled via hash query -> backend validates with parse_type_filter rejecting invalid -> panel must surface 422 not crash |
| Table rendering -> DOM | User-controlled incident fields (type/status strings) must be textContent only, not class injection beyond allowlist |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | Filter bar hash -> query forwarding | medium | mitigate | Type/status dropdown values are allowlist ["All","Bug","Feedback"] and ["All","Pending","In Progress","Resolved"] TitleCase; buildHash encodes; backend 422 handling shows inline error not raw detail; supports comma-separated status for future |
| T-03-06 | Denial of Service | Pagination Prev/Next rapid clicks | low | accept | Prev/Next disabled states prevent double-fetch at boundaries; page size fixed 20 prevents size>100 422; backend caps size le 100 |
| T-03-07 | Info disclosure | Empty state distinguishes "no incidents yet" vs "no results for filter" | low | mitigate | Both messages are generic, no data leakage; Clear Filters resets hash to remove filter params |
| T-03-SC | Tampering | npm deps | high | mitigate | No new deps; vitest/jsdom already verified; grep innerHTML block |
</threat_model>

<verification>
List: `vitest run --config panel/vitest.config.ts panel/src/views/list.test.ts -q` green with cases table, pagination, filter hash, skeleton 5, empty Clear Filters, error Retry; `curl -b cookies -s "http://localhost:8000/api/incidents?page=1&size=20&type=Bug&status=Pending"` filtered count matches; manual check hash #/incidents?type=Bug&status=Pending bookmarkable and reload preserves filters.
Responsive: Open /panel at 768px via Chrome device toolbar, table shows horizontal scroll or hidden thumbnail but badges visible; at 900 detail not needed here but list stays table.
Security: `grep -R innerHTML panel/src/views` 0; badge rendering uses textContent only.
</verification>

<success_criteria>
- Incident list paginated table with type/status/date/preview renders from {items,total,page,size,pages} size 20, row click to detail
- Filter bar Type and Status dropdowns reset page 1, update hash shareable driving backend ?type&status (case TitleCase, comma-separated)
- Footer Page X of N (Total T) with Prev/Next disabled correctly; fixed size 20; 422 and 429 handled
- Skeleton 5 rows while loading, empty illustration with Clear Filters localized, error with Retry re-fetches credentials:include
- Responsive tablet 768 hides thumbnail keeps badges, horizontal scroll fallback; all via textContent
</success_criteria>

<output>
Create .planning/phases/03-admin-panel/03-02-SUMMARY.md when done
</output>

## Artifacts this phase produces
- panel/src/views/list.ts — paginated filtered list view [creates]
- panel/src/components/badges.ts — badge renderers [creates]
- panel/src/components/skeleton.ts — skeleton rows [creates]
- panel/src/utils/format.ts — Intl date formatter [creates]
- panel/src/utils/sanitize.ts — href scheme guard [creates]
- panel/src/styles/components.css — badge/table/skeleton styles [modifies]
- panel/src/styles/responsive.css — 768 list compaction [modifies]
- panel/src/views/list.test.ts — list/pagination/filter/loading/empty tests [creates]
