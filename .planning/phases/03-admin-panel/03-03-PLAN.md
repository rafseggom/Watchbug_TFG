---
phase: 03-admin-panel
plan: "03"
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - panel/src/views/detail.ts
  - panel/src/components/toast.ts
  - panel/src/styles/components.css
  - panel/src/styles/responsive.css
  - panel/src/views/detail.test.ts
autonomous: true
requirements:
  - PAN-05
  - PAN-06
  - PAN-07
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Clicking an incident row navigates to #/incidents/:id and renders detail view: left pane large screenshot img src data:image/png;base64... from GET /api/incidents/:id (BYTEA re-encoded), max-width 100% object-fit contain, click to lightbox zoom; right pane metadata cards (URL as escaped link, User-Agent, viewport/resolution, timestamp localized, project_id, type/status badges), consoleLogs collapsible with level badge; top has Back to list and status dropdown"
    - "Status dropdown (Pending/In Progress/Resolved TitleCase) triggers immediate PATCH /api/incidents/:id/status with {status} and credentials:include; on 200 shows toast Status updated localized and optimistically updates local detail and cached list so back to list reflects new status without refetch; on 422/401/network shows inline error and reverts dropdown to prior value (Any→Any)"
    - "Detail stacks to single column on tablet below 900px (screenshot on top, metadata below full width); large screenshots scrollable and lazy; 404 invalid UUID or not found shows Incident not found localized with Back button; 401 during detail fetch triggers probe->refresh->login flow; network error shows Retry; all user fields via textContent and href guards"
  artifacts:
    - path: panel/src/views/detail.ts
      provides: Detail two-column layout with screenshot, metadata cards, consoleLogs, status PATCH, error/404 handling
    - path: panel/src/components/toast.ts
      provides: Shared toast for status updated and auth messages (already exists but extended)
    - path: panel/src/styles/components.css
      provides: Detail column, screenshot, metadata card, lightbox styles
    - path: panel/src/styles/responsive.css
      provides: Detail 900px stack media query
  key_links:
    - from: panel/src/views/detail.ts
      to: backend/app/routers/incidents.py GET /api/incidents/:id
      via: "apiFetch GET /api/incidents/:id with credentials:include -> IncidentOutDetail with screenshot data URL -> img src"
    - from: panel/src/views/detail.ts
      to: backend/app/routers/incidents.py PATCH /api/incidents/:id/status
      via: "select change -> PATCH {status: TitleCase} with credentials:include -> 200 optimistic update of cached list + toast else 422 revert"
    - from: panel/src/views/list.ts
      to: panel/src/views/detail.ts
      via: "row click location.hash #/incidents/:id and Back link location.hash #/incidents -> cached list reflects optimistic status without re-fetch"
---

<objective>
Incident detail view with full screenshot preview, metadata display, and status management, including responsive two-column → stacked layout and error handling. Delivers detail fetch, screenshot data URL rendering, formatted metadata cards, collapsible consoleLogs, immediate PATCH status workflow with optimistic update, 404/401/retry behavior, and PAN-07 escaped rendering.

Purpose: Satisfy PAN-05/06/07 detail excellence — users must open an incident, see visual proof, read metadata safely, and change status persistently.
Output: #/incidents/:id shows large screenshot with lightbox, metadata cards all via textContent, status dropdown persistently updates backend and reflects in list without refetch.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-panel/03-CONTEXT.md
@.planning/phases/03-admin-panel/03-RESEARCH.md
@.planning/phases/03-admin-panel/03-PATTERNS.md
@.planning/phases/03-admin-panel/03-VALIDATION.md
@.planning/phases/03-admin-panel/03-01-SUMMARY.md
@.planning/phases/03-admin-panel/03-02-SUMMARY.md
@backend/app/routers/incidents.py
@backend/app/services/incident_service.py
@backend/app/schemas/incident.py
</context>

<tasks>

<task type="auto">
  <name>Detail layout, screenshot, metadata cards, consoleLogs (escaped rendering)</name>
  <files>panel/src/views/detail.ts, panel/src/styles/components.css, panel/src/styles/responsive.css</files>
  <read_first>03-CONTEXT.md D-13/D-15/D-16, 03-RESEARCH.md Patterns 7-8 + Pitfalls 6,8 + Code Examples img+textContent, backend/app/services/incident_service.py:encode_screenshot, backend/app/schemas/incident.py:IncidentOutDetail, backend/app/routers/incidents.py:139-168, panel/src/api.ts, panel/src/i18n/index.ts, panel/src/utils/format.ts, panel/src/utils/sanitize.ts</read_first>
  <acceptance_criteria>Dedicated hash route #/incidents/:id renders two-column desktop (screenshot 60% left, metadata 40% right) and stacks vertically below 900px (screenshot top full width); screenshot from GET /api/incidents/:id detail endpoint as img src data:image/png;base64... with max-width 100% object-fit contain, click opens lightbox overlay dismissable, lazy loading; metadata as labeled cards: URL rendered as &lt;a&gt; with textContent + href only if isSafeHref (reject javascript:), other fields User-Agent/viewport/resolution/timestamp/project_id/type/status badges via textContent, timestamp localized via formatDate; consoleLogs (Bug required, Feedback optional per TRN-04) as collapsible details per entry with level badge log/warn/error/info color + args joined as textContent + timestamp; top has Back to list link to #/incidents and skeleton while loading; missing UUID 404 or invalid shows Incident not found with Back button localized; all via textContent never innerHTML</acceptance_criteria>
  <action>Per D-13 (dedicated hash route two-col costly), D-15 (formatted cards + collapsible consoleLogs escaped reversible), D-16 (responsive stack + 404/401 reversible), PAN-05/07 and RESEARCH Patterns 7-8:
- Create panel/src/views/detail.ts with export async function renderDetail(root:HTMLElement, id:string) that: clears root, renders header via renderHeader, creates top bar with Back link (&lt;a&gt; href="#/incidents" textContent=t("detail.back")) and placeholder status dropdown container; creates two-col container div.detail-layout with left pane div.screenshot-pane and right pane div.metadata-pane; shows skeleton or spinner until fetch; does await apiFetch(`/api/incidents/${id}`,{credentials:"include"}?) actually uses apiFetch with credentials:include hard-coded; if response 404 then render 404 page div with p.textContent=t("detail.notFound") and button Back, return; if 401 then let apiFetch retry via refresh path exact /api/auth/refresh on 401 delegated; if network error show inline error with Retry button re-calling renderDetail; on 200 json detail = {id,type,status,created_at,payload:{metadata:{url,userAgent,viewport,resolution,timestamp,project_id}, consoleLogs?, screenshot?}, has_screenshot?(maybe), screenshot:dataURL}; left pane: create img with src=detail.screenshot (data:image/png;base64...) [VERIFIED: incident_service.py encode_screenshot returns data URL], set max-width 100% object-fit contain, loading="lazy", click -> create lightbox overlay div.lightbox with img clone max-width 90vw, click outside dismiss; right pane: create metadata cards as div.card per field: label textContent via t("detail.labels.url") etc, value via textContent, for URL field create &lt;a&gt; with a.textContent=url and if isSafeHref(url) then a.href=url else no href (show text only) to block javascript: per Pitfall 6; User-Agent/resolution/timestamp via textContent, timestamp via formatDate(detail.payload.metadata.timestamp,getLang()); type/status badges via renderTypeBadge/statusBadge; project_id textContent; consoleLogs section: if detail.payload.consoleLogs array then create details.collapsible with summary textContent=`ConsoleLogs (${n})` and for each entry create div.log-entry with span.level badge class log-(level) textContent=level and span.args textContent=args.join(" ") and span.ts textContent=entry.timestamp via formatDate, all textContent; notes if present. Do not render raw JSON &lt;pre&gt;; truncate consoleLogs after N with Show more if &gt;20 per agent discretion. Ensure image scrollable container with overflow auto for large screenshots.
- Update panel/src/styles/components.css with .detail-layout {display:flex; gap:24px} .screenshot-pane {flex: 0 0 60%; overflow:auto} .metadata-pane {flex:1} .card {border:1px solid var(--border); border-radius:8px; padding:12px; margin-bottom:12px} .lightbox {position:fixed; inset:0; background:rgba(0,0,0,.8); display:flex; align-items:center; justify-content:center} and badge classes already defined. Update responsive.css with @media (max-width:900px){.detail-layout{flex-direction:column} .screenshot-pane,.metadata-pane{flex:auto; width:100%}} per D-16 900px stack; keep 768 for list.
- Handle 401 during detail fetch via apiFetch refresh path — if still 401 after retry, redirect location.hash="#/login" same as guard D-06.
- All rendering via createElement+textContent; do not set innerHTML for any card or consoleLogs arg.
</action>
  <verify>
    <automated>vitest run --config panel/vitest.config.ts panel/src/views/detail.test.ts -t "detail|metadata|screenshot" --reporter verbose 2>&1 | tail -40</automated>
  </verify>
  <done>Detail renders two-col stacked below 900, screenshot data URL with lightbox, metadata cards and consoleLogs collapsible all via textContent, 404 not found page, 401 triggers guard refresh->login, network Retry.</done>
</task>

<task type="auto">
  <name>Status PATCH workflow with optimistic update, toast, and error revert</name>
  <files>panel/src/views/detail.ts, panel/src/components/toast.ts, panel/src/views/detail.test.ts</files>
  <read_first>03-CONTEXT.md D-14 (immediate PATCH + toast + optimistic reversible), 03-RESEARCH.md Pitfalls 5,7, backend/app/routers/incidents.py:170-218, backend/app/schemas/incident.py:StatusUpdate Literal Pending/In Progress/Resolved, panel/src/api.ts, panel/src/i18n/en.json, panel/src/views/list.ts cached list</read_first>
  <acceptance_criteria>Status select has options All? No — only Pending, In Progress, Resolved TitleCase matching StatusUpdate literal per Pitfall 5; current status selected; on change does PATCH /api/incidents/:id/status body {status: "Resolved"} with credentials:include; on 200 shows localized toast Status updated, optimistically updates local detail state and cached list entry (so back to list shows new status without refetch); on 422 invalid status / 401 / network shows inline error and reverts dropdown to prior value; Any→Any transition allowed no state-machine check per Phase 2 D-12; re-entering detail after list back still shows updated status</acceptance_criteria>
  <action>Per D-14 (immediate PATCH on dropdown change + toast + optimistic list update reversible) and RESEARCH Pitfall 5:
- Enhance panel/src/views/detail.ts to add status dropdown in top bar or metadata: create select.status-select with const ALLOWED=["Pending","In Progress","Resolved"] (mirrors backend ALLOWED_STATUSES [VERIFIED: pagination.py]), each option value TitleCase and textContent localized via t("status.Pending") or raw; set select.value = detail.status; add change listener: const prior = detail.status; const next = select.value; disable select, show inline error placeholder div; try { const res=await apiFetch(`/api/incidents/${id}/status`,{method:"PATCH", body:JSON.stringify({status:next})}); if(res.ok){detail.status=next; showToast(t("toast.statusUpdated")); /* optimistic list cache */ try{ window.dispatchEvent(new CustomEvent("watchbug:status-updated",{detail:{id,status:next}})); localStorage.setItem(`watchbug:inc-${id}:status`,next); }catch{}; // list view listens or next render reads cached; also update any in-memory map window.__WB_LIST_CACHE if exists } else if(res.status===422){const err=await res.json(); inlineError.textContent=t("errors.invalidStatus")|| err.detail; select.value=prior; showToast(t("errors.invalidStatus"),"error");} else if(res.status===401){ // delegate to refresh retry via apiFetch already, if still 401 then location.hash="#/login" } else {inlineError.textContent=t("errors.network"); select.value=prior;} } catch(e){inlineError.textContent=t("errors.network"); select.value=prior;} finally{select.disabled=false;}
- Add optimistic list update mechanism: in list view, maintain map id->status in module scope or localStorage; on renderList, if localStorage has entry for id overlay with cached status before fetch or after; on watchbug:status-updated event, update cached rows without refetch. Simpler: after PATCH success store in localStorage as above; list view on next mount reads it and prefers over fetched if newer. Document caching in comments.
- Create panel/src/views/detail.test.ts with tests: test "detail renders status dropdown with TitleCase options and current selected" mock GET detail returns status Pending, assert select.options TitleCase and value Pending; test "PATCH 200 optimistic update shows toast and caches" mock PATCH 200, fire change to Resolved, assert fetch called with credentials:include, method PATCH, body {status:"Resolved"}, showToast called with localized, select remains Resolved, localStorage cached; test "PATCH 422 reverts to prior" mock 422 detail loc ["body","status"], assert error inline and select.value reverted to Pending; test "xss via metadata url javascript: scheme not set as href" create detail with url javascript:alert(1) assert a.href not javascript and textContent shows url; test "skeleton while loading detail" before fetch resolves shows loading. Use jsdom and vi.spyOn fetch/global.
- Ensure panel/src/components/toast.ts is imported and showToast uses textContent; do not add new deps.
- Ensure 422 loc shape ["body","status"] per backend routers/incidents.py:211 loc ("body",)+loc is shown correctly as field error if needed but generic Invalid status suffices for UI per D-14.
</action>
  <verify>
    <automated>vitest run --config panel/vitest.config.ts panel/src/views/detail.test.ts -t "status patch|optimistic|revert" --reporter verbose 2>&1 | tail -40; grep -rn "innerHTML" panel/src --quiet && echo "FAIL innerHTML" || echo "no innerHTML"</automated>
  </verify>
  <done>Status PATCH dropdown TitleCase immediate, 200 toast optimistic cache reflects in list without refetch, 422 reverts with error, Any→Any allowed, all via textContent and href guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Detail screenshot -> img src | Data URL from BYTEA re-encoded via encode_screenshot is not user HTML — safe for img src; still max-width 100% contain, no innerHTML |
| Status PATCH -> API | User can try to send lowercased status pended; Pydantic rejects 422; panel must send TitleCase and handle 422 gracefully |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | Screenshot img src data URL | medium | mitigate | Encode_screenshot returns data:image/png;base64,... validated Base64 from BYTEA; panel sets img.src directly without decode; no user HTML ever reaches img src; large images lazy and scrollable |
| T-03-09 | Tampering | ConsoleLogs args rendering | high | mitigate | Each arg joined and set via textContent; level badge via allowlist class log/warn/error/info; never innerHTML; backend html.escape before JSONB is primary gate |
| T-03-10 | Tampering | Status PATCH Any→Any (no state-machine) | low | accept | Backend allows Any→Any per Phase 2 D-12; panel immediate PATCH is valid; future state-machine can be added with Save button per D-14 reversible note — no validation needed now |
| T-03-11 | Info disclosure | Detail 404 vs 422 distinction | low | mitigate | 404 shows generic Incident not found with Back, 422 shows inline Invalid status not raw loc array; no stack trace leaked |
| T-03-SC | Tampering | deps | high | mitigate | No new deps; vitest reuse; grep innerHTML gate |

</threat_model>

<verification>
Detail: `vitest run --config panel/vitest.config.ts panel/src/views/detail.test.ts -q` green with detail, metadata XSS, status patch optimistic, revert, screenshot lightbox; manual detail open shows two-column desktop and stacked below 900; click row -> detail shows screenshot full and metadata; status change reflects in list via back navigation without refetch.
Responsive: Resize to 900 and 768 via Chrome device toolbar verifies stack and list compaction; no horizontal overflow for metadata cards.
Security: `grep -R innerHTML panel/src` 0; javascript: URL not set as href via test; consoleLogs Xss payload &lt;script&gt; renders as textContent.
</verification>

<success_criteria>
- Dedicated #/incidents/:id two-column (60/40) stacks below 900, screenshot data URL full preview with lightbox, metadata cards and consoleLogs all via textContent
- Status dropdown Pending/In Progress/Resolved immediate PATCH with credentials:include 200->toast+optimistic list, 422->revert with error, Any→Any
- 404 not found with Back, 401 via guard refresh->login, network Retry, detail screenshot lazy scrollable, locale date
</success_criteria>

<output>
Create .planning/phases/03-admin-panel/03-03-SUMMARY.md when done
</output>

## Artifacts this phase produces
- panel/src/views/detail.ts — detail 2-col + lightbox + error handling [creates]
- panel/src/components/toast.ts — extended or reused [modifies]
- panel/src/styles/components.css — detail layout + lightbox [modifies]
- panel/src/styles/responsive.css — 900 stack [modifies]
- panel/src/views/detail.test.ts — detail, XSS, status patch, optimistic tests [creates]
