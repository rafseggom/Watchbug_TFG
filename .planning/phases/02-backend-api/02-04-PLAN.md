---
phase: 02-backend-api
plan: "04"
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - backend/app/utils/pagination.py
  - backend/app/schemas/incident.py
  - backend/app/schemas/common.py
  - backend/app/routers/incidents.py
  - backend/app/services/incident_service.py
  - backend/app/main.py
  - backend/tests/test_incidents_list.py
  - backend/tests/test_incidents_status.py
  - backend/tests/conftest.py
autonomous: true
requirements:
  - API-03
  - API-04
  - AUTH-03
  - SEC-01
estimate:
  tokens: 56000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Authenticated GET /api/incidents returns paginated {items, total, page, size, pages} with default page 1 size 20 max 100, filterable by type Bug/Feedback and status Pending/In Progress/Resolved comma-separated, combination, case-insensitive for type, ordered by created_at desc
    - GET /api/incidents without JWT cookie returns 401; with valid cookie but invalid filter values returns 400/422; pagination pages = ceil(total/size)
    - PATCH /api/incidents/:id/status with body {status: "Resolved"} allows Any->Any among three states and returns 200 {id, status}; invalid id returns 404, invalid status returns 422, without auth returns 401
    - List endpoint excludes screenshot BYTEA for performance (no OOM, 20 rows fast); detail GET /api/incidents/:id or screenshot field re-encodes Base64 data URL for Panel img src
    - GET /api/health remains public and DOCS_ENABLED gates /docs/openapi.json behind JWT in production per D-16
  artifacts:
    - backend/app/utils/pagination.py
    - backend/app/routers/incidents.py
    - backend/app/services/incident_service.py
    - backend/tests/test_incidents_list.py
    - backend/tests/test_incidents_status.py
  key_links:
    - GET /api/incidents -> Depends(get_current_user) -> query builder select count + offset/limit -> PaginatedResponse
    - PATCH /api/incidents/{id}/status -> Depends(get_current_user) -> enum validation -> UPDATE incidents set status -> return id/status
    - List excludes LargeBinary via load_only or deferred or explicit column selection; detail encodes BYTEA via base64.b64encode
    - Docs gate: if DOCS_ENABLED false set docs_url/openapi_url None else protect via dependency or leave public in dev
---

<objective>
Deliver retrieval and status management: paginated, filterable incident listing and Any->Any status updates, both protected by JWT, with performance-safe BYTEA handling and docs gating. Completes API-03, API-04, and verifies AUTH-03 on real retrieval flows.

Purpose: Panel (Phase 3) depends on page/size/total/pages with type/status filters and status PATCH per D-09/D-10/D-12; without these, admin workflow is impossible (Success Criteria 3-4).
Output: Working GET /api/incidents?page&size&type&status returning filtered paginated results and PATCH /api/incidents/:id/status updating status, both requiring JWT, with docs gated and health public.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-PATTERNS.md
@sdk/src/transport/sender.ts
</context>

<tasks>

<task type="auto" reversibility="costly">
  <name>Paginated filtered incident retrieval (list without BYTEA)</name>
  <files>backend/app/utils/pagination.py, backend/app/schemas/common.py, backend/app/schemas/incident.py, backend/app/services/incident_service.py, backend/app/routers/incidents.py</files>
  <read_first>02-CONTEXT.md D-09 pagination page/size/total/pages + D-10 filtering comma-separated Any type/status + SEC-01 admin allowlist, 02-RESEARCH.md Patterns 12 pagination+filtering + Pitfall 7 BYTEA OOM + Pitfall 9 type case, 02-PATTERNS.md utils/pagination.py + routers/incidents.py assignments, backend/app/dependencies.py, backend/app/models/incident.py</read_first>
  <acceptance_criteria>With 25 seeded incidents (mix Bug/Feedback, statuses Pending/In Progress/Resolved), GET /api/incidents with auth cookie and no query returns 200 with {items: 20, total: 25, page:1, size:20, pages:2} items ordered created_at desc and items do NOT contain screenshot bytes (or contain has_screenshot bool); GET ?page=2&size=10 returns items length 10 page 2; GET ?size=101 returns 422 validation error max 100; GET ?type=Bug returns only Bug rows; GET ?type=feedback (lowercase) normalized also returns Bug; GET ?type=Bug,Feedback returns both; GET ?status=Pending returns only Pending; GET ?status=Pending,In%20Progress with comma-encoded returns both; GET ?type=Bug&status=Pending combined filters to intersection; total/pages math correct ceil; unauthenticated GET returns 401; list performs SELECT COUNT(*)+offset/limit not full table load</acceptance_criteria>
  <action>Per D-09 costly (cursor vs page/size change requires Panel rewrite) and Pitfalls:
- Create backend/app/utils/pagination.py with helper paginate_query or inline in service: async def paginate_and_filter(db, page int, size int, type_filter str|None, status_filter str|None) -> (items, total, pages): parse type_filter split by "," strip, title normalize via {"bug":"Bug","feedback":"Feedback"} lower mapping, validate against allowed ["Bug","Feedback"], filters list appended with Incident.type.in_(types); similarly status split strip validate against ["Pending","In Progress","Resolved"] (note In Progress has space, so comma split must preserve space, test uses "Pending,In Progress" -> split correctly). Build count_q = select(func.count()).select_from(Incident).where(*filters) -> total scalar_one. Compute pages = (total + size -1)//size if total else 0. Build items_q = select(Incident).where(*filters).order_by(Incident.created_at.desc()).offset((page-1)*size).limit(size). Use load_only or deferred to exclude screenshot LargeBinary from list query: e.g., options(load_only(Incident.id, Incident.type, Incident.status, Incident.payload, Incident.project_id, Incident.created_at, Incident.updated_at)) or simply not selecting screenshot column but ORM loads all by default so use query. Ensure not to load BYTEA for all 20 rows (Pitfall 7 OOM). Execute and scalars all.
- Update backend/app/schemas/common.py PaginatedResponse to generic or IncidentOut list with fields id type status payload project_id created_at updated_at (no screenshot or has_screenshot bool), use ConfigDict from_attributes True.
- Update backend/app/schemas/incident.py IncidentOut to exclude screenshot or include screenshot_url/computed field; list returns without screenshot, detail will include base64 encoded variant.
- Update backend/app/services/incident_service.py with list_incidents function implementing above plus helper to_incident_out mapping Incident -> dict without BYTEA decode.
- Update backend/app/routers/incidents.py GET /api/incidents handler signature: async def list_incidents(request Request, user User Depends(get_current_user), db AsyncSession Depends(get_db), page int Query(1, ge=1), size int Query(20, ge=1, le=100), type str|None Query(None, description="Bug or Feedback comma-separated"), status str|None Query(None, ...) ) -> dict: verify auth via dependency (AUTH-03), apply rate limiter @limiter.limit("60/minute"), implement filter+pagination via service, return items total page size pages. Ensure Query le 100 enforced per D-09 max.
- Add rate limiting decorator and CORS admin path already handled.
- Do NOT expose list without auth; verify 401 before query builder runs.
</action>
  <verify>
    <automated>pytest backend/tests/test_incidents_list.py -k "paginated or filter or unauth or size_cap" -xvs 2>&1 | tail -70</automated>
  </verify>
  <done>Paginated GET returns correct total/pages/items without BYTEA, filters by type/status comma-separated combined and case-insensitive type normalized, size capped 100, unauth 401, ordering desc.</done>
</task>

<task type="auto">
  <name>Status update and detail with BYTEA re-encode + docs gating</name>
  <files>backend/app/routers/incidents.py, backend/app/services/incident_service.py, backend/app/main.py, backend/app/schemas/incident.py</files>
  <read_first>02-CONTEXT.md D-12 Any->Any status PATCH + D-16 health public docs gated, 02-RESEARCH.md Pattern 13 BYTEA encode + Open Questions Q1 list vs detail + Security Domain V13 docs gated, 02-PATTERNS.md routers/incidents.py PATCH outline + ARCHITECTURE docs gate, backend/app/utils/sanitize.py</read_first>
  <acceptance_criteria>PATCH /api/incidents/{id}/status with valid uuid and body {status:"In Progress"} returns 200 {id, status:"In Progress"} and subsequent GET detail shows status updated; PATCH with invalid uuid returns 404; PATCH with invalid status like "Closed" returns 422; PATCH without auth returns 401; status Any->Any allowed e.g., Resolved -> Pending succeeds (no state machine); GET /api/incidents/{id} (detail) returns full payload plus screenshot re-encoded as Base64 data URL (valid image bytes match original upload); GET /api/health remains public without auth; /docs is 404 when DOCS_ENABLED false and returns docs when true (or gated behind JWT in prod)</acceptance_criteria>
  <action>Per D-12 reversible Any->Any and D-16 reversible docs gating:
- Update backend/app/routers/incidents.py to add: GET /api/incidents/{id} detail endpoint with Depends(get_current_user), path param id uuid.UUID, fetch via await db.get(Incident, id) or select, if not found raise HTTPException 404 detail incident not found, else return IncidentOutDetail including screenshot re-encoded: base64.b64encode(incident.screenshot).decode("ascii") prefixed with "data:image/png;base64," or raw base64 per decide; choose data URL for Panel img src direct use. Exclude raw BYTEA streaming variant for now but ensure encode helper exists.
- PATCH /api/incidents/{id}/status: define StatusUpdate schema with status Literal["Pending","In Progress","Resolved"] or str with validator that allows only those 3 title values (normalize but keep exact). Path id uuid.UUID, body StatusUpdate, dependencies get_current_user and get_db plus rate limiter @limiter.limit("60/minute"). Logic: fetch incident by id, 404 if not found, set incident.status = body.status, updated_at auto via onupdate, commit refresh, return {"id": str(incident.id), "status": incident.status}. No state-machine check Any->Any per D-12. Return 200.
- Handle invalid UUID path: FastAPI auto returns 422 for bad uuid format; add test for 422 vs 404 distinction.
- Update backend/app/main.py docs gate per D-16: if get_settings().DOCS_ENABLED false, app docs_url redoc_url openapi_url remain None (no docs); if true, enable docs at /docs with default; optionally protect docs behind JWT in prod by adding dependency but for v1 gating via flag is sufficient per research. Ensure health endpoint stays public.
- Update backend/app/services/incident_service.py helpers: encode_screenshot(raw bytes) -> str base64, decode variant already exists, update_status service function if extracted.
- Ensure XSS sanitization preserved on status update (status is enum not user text, no sanitize needed).
</action>
  <verify>
    <automated>pytest backend/tests/test_incidents_status.py -xvs 2>&1 | tail -50</automated>
  </verify>
  <done>PATCH status Any->Any returns 200, 404 on missing id, 422 on bad status, 401 unauth; detail GET returns Base64 screenshot correctly re-encoded; health public; docs gated by DOCS_ENABLED flag.</done>
</task>

<task type="auto">
  <name>Retrieval tests and E2E phase verification</name>
  <files>backend/tests/test_incidents_list.py, backend/tests/test_incidents_status.py, backend/tests/conftest.py</files>
  <read_first>02-RESEARCH.md Validation Architecture Test Map for API-03/04 + Sampling Rate, 02-PATTERNS.md tests/test_*.py patterns, 02-CONTEXT.md D-09/D-10 filter shapes + Success Criteria 3/4, backend/app/routers/incidents.py</read_first>
  <acceptance_criteria>Full suite with real PG: pytest backend/tests/ -v passes including test_health, test_auth, test_incidents_ingest, test_incidents_list (paginated, filter_type, filter_status, combined, case_insensitive, ordering, size_cap, unauth), test_incidents_status (patch ok, not found, invalid enum, unauth, Any->Any transition), test_security (cors, rate limit, xss, size). E2E flow as single pytest scenario: login -> POST ingest as public with key -> GET list with cookie -> PATCH status -> GET detail verify screenshot -> logout verify 401. Coverage proves Success Criteria 3-5.</acceptance_criteria>
  <action>Per Validation Architecture sampling rate and per-task verify rules:
- Create/complete backend/tests/test_incidents_list.py with 8+ tests using conftest seeded fixtures: create 25 incidents via POST or direct DB for speed, then test_paginated_ok (page1 size20 pages 2), test_paginated_page2 (page2 size10), test_size_cap_422 (size 101 ->422), test_filter_type_bug, test_filter_type_lowercase_normalized, test_filter_type_comma, test_filter_status_pending, test_filter_status_comma_with_space (Pending,In Progress), test_filter_combined_type_status, test_ordering_desc, test_unauth_401. Verify response shape keys items total page size pages.
- Create/complete backend/tests/test_incidents_status.py with 6+ tests: test_patch_status_ok (Pending->In Progress), test_patch_resolved_then_pending_any_to_any, test_patch_not_found_404 random uuid, test_patch_invalid_status_422 (Closed), test_patch_unauth_401, test_patch_invalid_uuid_422, test_detail_includes_screenshot_base64 (GET detail returns screenshot that decodes to same bytes as original), test_health_still_public after auth.
- Update backend/tests/conftest.py to add helpers for login and auth header injection, seed multiple incidents helper.
- Run full suite pytest backend/tests -q full, then pytest --cov=app --cov-report=term-missing -v for wave merge, ensure no 3 consecutive tasks without automated verify, feedback latency <2s for quick runs.
- Document expected docker-compose health: pgdata volume persistence not needed in this phase but ensure alembic upgrade head run is blocking before verify (already in Plan01).
</action>
  <verify>
    <automated>pytest backend/tests/ -v 2>&1 | tail -80</automated>
  </verify>
  <done>All list/filter/pagination/status tests green with real PG, E2E login-ingest-list-patch-detail-logout flow passes, health public, docs gating verified, no regression on security/ingest/auth tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| cookie->GET list / PATCH status | Authenticated but must still validate filters and status enum to prevent injection |
| DB -> list query | Must not leak BYTEA bulk; must use bound params not string interpolation |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | ASVS |
|-----------|----------|-----------|----------|-------------|-----------------|------|
| T-02-04-01 | Tampering | SQL injection via type/status filter | high | mitigate | SQLAlchemy select where Incident.type.in_(types) with bound params, plus Pydantic/validator allowlist before query; no string formatting | V5 |
| T-02-04-02 | Tampering | status enum bypass | medium | mitigate | StatusUpdate Literal["Pending","In Progress","Resolved"] with strict validation, 422 on invalid; Any->Any allowed but only within 3 values | V5 |
| T-02-04-03 | Information disclosure | BYTEA OOM leak via list | medium | mitigate | List query excludes LargeBinary via load_only/deferred or not selecting screenshot; detail streams or re-encodes per row only | V11 |
| T-02-04-04 | Spoofing | IDOR on PATCH id | medium | mitigate | Validate JWT before fetching; UUID path param validated, 404 not 403 to avoid enumeration but auth required first | V4 |
| T-02-04-05 | Info disclosure | docs exposure in prod | low | accept | Docs gated by DOCS_ENABLED flag; disabled by default in prod; rationale low risk self-hosted single admin but must document | V13 |
| T-02-04-SC | Tampering | pip installs | high | mitigate | Verified deps, no postinstall scripts | V14 |
</threat_model>

<verification>
`pytest backend/tests/ -v --cov=app` green, all 5 success criteria manually: `curl -b cookies http://localhost:8000/api/incidents?page=1&size=20&type=Bug&status=Pending` returns paginated filtered; `curl -X PATCH http://localhost:8000/api/incidents/<id>/status -H "Content-Type: application/json" -d '{"status":"Resolved"}' -b cookies` returns 200; unauth same without cookies returns 401; list response does not contain huge screenshot string (verify length <2KB per item).
</verification>

<success_criteria>
- GET /api/incidents paginated filterable by type (Bug/Feedback case-insensitive) and status (Pending/In Progress/Resolved comma-separated combined) returns {items,total,page,size,pages} pages ceil correct, size max 100 422, ordered desc, unauth 401
- List excludes BYTEA (fast, no OOM), detail re-encodes Base64 valid data URL
- PATCH status Any->Any succeeds, invalid id 404 invalid status 422 unauth 401
- Health remains public GET /api/health ok/connected|disconnected, docs gated by DOCS_ENABLED
- Full pytest suite green with real PostgreSQL including auth, ingest, list, status, security; E2E login-ingest-list-patch-detail flow passes
</success_criteria>

<output>
Create .planning/phases/02-backend-api/02-04-SUMMARY.md when done
</output>

## Artifacts this phase produces
- backend/app/utils/pagination.py — helper paginate_and_filter with count + ceil pages + offset/limit + load_only BYTEA exclusion [creates]
- backend/app/schemas/common.py — updated PaginatedResponse generic with IncidentOut list [modifies]
- backend/app/schemas/incident.py — updated IncidentOut without screenshot + IncidentOutDetail with screenshot_base64, StatusUpdate Literal enum [modifies]
- backend/app/services/incident_service.py — added list_incidents, update_status, to_incident_out, encode_screenshot helpers [modifies]
- backend/app/routers/incidents.py — added GET /api/incidents paginated filtered with auth+limiter + GET /api/incidents/{id} detail with BYTEA encode + PATCH /api/incidents/{id}/status Any->Any [modifies]
- backend/app/main.py — updated docs gate DOCS_ENABLED check if needed [modifies if not already]
- backend/tests/conftest.py — updated with multi-incident seed helpers and detail decode check [modifies]
- backend/tests/test_incidents_list.py — 8+ tests paginated, filter, combined, case-insensitive, ordering, size cap, unauth [creates]
- backend/tests/test_incidents_status.py — 6+ tests patch ok/404/422/unauth/any->any, detail screenshot [creates]
