---
phase: 02-backend-api
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - backend/app/services/auth_service.py
  - backend/app/dependencies.py
  - backend/app/routers/auth.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/tests/test_auth.py
  - backend/tests/conftest.py
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - DB-02
  - SEC-05
estimate:
  tokens: 54000
  raw_tokens: 27000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Admin can POST /api/auth/login with email/password and receives watchbug_access (1h) + watchbug_refresh (7d) HttpOnly cookies SameSite Lax Secure toggled by ENV; wrong password returns 401 invalid credentials
    - Passwords stored as bcrypt hash (cost 12) never plaintext; seed admin upserts from ADMIN_EMAIL/ADMIN_PASSWORD on lifespan startup idempotently and rotates hash if password changed
    - All GET /api/incidents and PATCH /api/incidents/* without valid watchbug_access cookie return 401 not authenticated / token expired; with valid cookie they succeed
    - POST /api/auth/refresh with valid refresh cookie reissues access cookie; POST /api/auth/logout clears both cookies via Max-Age 0
    - .env.example documents every Settings field; no secrets hardcoded in Python; JWT uses HS256 with PyJWT and algorithms allowlist
  artifacts:
    - backend/app/services/auth_service.py
    - backend/app/dependencies.py
    - backend/app/routers/auth.py
    - backend/tests/test_auth.py
  key_links:
    - Login -> bcrypt.checkpw -> jwt.encode sub/jti/exp/iat HS256 -> set_cookie HttpOnly SameSite Lax Secure per ENV
    - get_current_user reads watchbug_access cookie -> jwt.decode algorithms HS256 -> DB lookup user id -> 401 on missing/expired/invalid
    - Lifespan startup -> seed_admin(email,password) -> hash_password gensalt 12 -> upsert users table
    - Logout -> set_cookie Max-Age 0 on both cookies -> browser stops sending; no server denylist
---

<objective>
Wire JWT HttpOnly cookie auth per D-01..D-04: bcrypt password hashing, HS256 1h/7d tokens with jti/sub/exp, login/refresh/logout endpoints, seeded admin from .env, and get_current_user dependency that protects GET/PATCH incidents. Completes AUTH-01..04 and SEC-06.

Purpose: Without auth, retrieval and status updates cannot be secured (AUTH-03, Success Criterion 5). Seeded admin avoids open registration attack surface.
Output: Working login flow that sets namespaced HttpOnly cookies, protected routes returning 401 when unauthenticated, logout clearing cookies, and bcrypt-verified admin seed.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-PATTERNS.md
@sdk/src/transport/sender.ts
</context>

<tasks>

<task type="auto" reversibility="one-way">
  <name>Auth service and dependencies: bcrypt + JWT HS256 + seed</name>
  <files>backend/app/services/auth_service.py, backend/app/dependencies.py, backend/app/config.py</files>
  <read_first>02-CONTEXT.md D-01..D-04 (HttpOnly cookie, 1h/7d, seeded admin, logout), 02-RESEARCH.md Patterns 5 (JWT cookie) + 6 (bcrypt direct) + Code Example Protected Route Dependency, 02-PATTERNS.md services/auth_service.py + dependencies.py assignments, backend/app/db.py, backend/app/models/user.py</read_first>
  <acceptance_criteria>hash_password produces $2b$12$... 60-char hash distinct per call due to salt; verify_password true for matching and false otherwise; create_access_token creates HS256 token with sub=jti UUID exp now+60min iat now decode succeeds with algorithms HS256 and fails with wrong secret; create_refresh_token same with 7 days and type refresh claim; seed_admin inserts user when missing and updates password_hash when ADMIN_PASSWORD rotated (verify_password after rotation succeeds, old fails); get_current_user with missing cookie raises 401 not authenticated, expired token 401 token expired, invalid signature 401 invalid token</acceptance_criteria>
  <action>Per D-03 reversibility one-way (users table is canonical, no open registration) and D-01 costly cookie-only:
- Create backend/app/services/auth_service.py: implement hash_password(plain str) -> str via bcrypt.hashpw(plain.encode utf8, bcrypt.gensalt(rounds=12)).decode, verify_password(plain, hashed) via bcrypt.checkpw encode both, create_access_token(user_id str, secret str) builds now datetime.now(timezone.utc), payload {"sub": user_id, "jti": str(uuid.uuid4()), "exp": now+timedelta(hours=1), "iat": now} via jwt.encode payload, secret, algorithm HS256, create_refresh_token same with 7 days + type refresh, verify_token via jwt.decode with algorithms ["HS256"], plus helper decode wrapper that maps ExpiredSignatureError->token expired and InvalidTokenError->invalid token. Also implement seed_admin(db AsyncSession, email str, password str) async: execute select User where email, if none add User(email, password_hash=hash_password(password)), elif not verify_password(password, user.password_hash) then user.password_hash=hash_password(password), commit. Use direct bcrypt not passlib per RESEARCH Alternatives Considered; ensure no passlib import.
- Create backend/app/dependencies.py: implement async get_current_user(request Request, db AsyncSession Depends(get_db)) -> User: reads request.cookies.get("watchbug_access") (namespaced per Agent Discretion watchbug_access/watchbug_refresh), if missing raise HTTPException 401 not authenticated, try jwt.decode token, get_settings().JWT_SECRET, algorithms ["HS256"] (allowlist, never decode without), except ExpiredSignatureError raise 401 token expired, except InvalidTokenError raise 401 invalid token, then import uuid and do await db.get(User, uuid.UUID(payload["sub"])) if not user raise 401 user not found, return user. Also export require_auth alias for router.
- Update backend/app/config.py if needed: ensure ENV or DEBUG field exists to toggle Secure flag (e.g., ENV str default development, DEBUG bool). Add field if missing; keep HS256 default per Agent Discretion.
- Do NOT accept Authorization Bearer header anywhere; cookie-only per D-01. Ensure jwt import is pyjwt (import jwt) not python-jose. Never log secrets.
- Must work on lifespan startup: seed_admin will be called from main lifespan later, but service must be importable without circular deps.
</action>
  <verify>
    <automated>pytest backend/tests/test_auth.py::test_password_is_hashed -xvs 2>&1 | tail -30; pytest backend/tests/test_auth.py::test_login_sets_cookies -xvs 2>&1 | tail -40</automated>
  </verify>
  <done>auth_service hashes with bcrypt cost12, jwt encode/decode HS256 with jti/sub/exp/iat and refresh type, seed_admin upsert verified, get_current_user dependency enforces cookie auth with correct 401 messages, no Authorization header checked.</done>
</task>

<task type="auto" reversibility="costly">
  <name>Auth routers: login, refresh, logout with HttpOnly cookies and protected wiring</name>
  <files>backend/app/routers/auth.py, backend/app/main.py, backend/app/config.py</files>
  <read_first>02-RESEARCH.md Pattern 5 JWT HttpOnly Cookie Auth full example (lines 442-489) + Pitfall 5 Secure cookie on localhost, 02-PATTERNS.md routers/auth.py assignment, 02-CONTEXT.md D-01/D-02 cookie names and SameSite Lax, backend/app/services/auth_service.py, backend/app/dependencies.py</read_first>
  <acceptance_criteria>POST /api/auth/login with valid seeded admin creds returns 200 {message logged in} and Set-Cookie headers for watchbug_access (HttpOnly, SameSite Lax, Path /, Max-Age 3600) and watchbug_refresh (Path /api/auth, Max-Age 604800) with Secure=False when ENV=development and True when production; invalid creds returns 401 invalid credentials; POST /api/auth/refresh with valid refresh cookie returns 200 and reissues access cookie; POST /api/auth/logout clears both cookies with Max-Age 0 and returns logged out; GET /api/incidents without cookie returns 401, with access cookie returns 200</acceptance_criteria>
  <action>Per D-01 costly (switching to header auth requires Panel fetch rewrite) and D-02 reversible token lifetimes:
- Create backend/app/routers/auth.py APIRouter prefix /api/auth: POST /login takes LoginRequest body, db AsyncSession Depends(get_db), Response; lookup user via select User where email, if missing or bcrypt.checkpw fails raise HTTPException 401 invalid credentials (generic to avoid user enumeration), else create tokens via auth_service, set cookies via response.set_cookie(key="watchbug_access", value=access, httponly True, secure= (settings.ENV=="production"), samesite="lax", max_age=3600, path="/"), second cookie watchbug_refresh httponly True secure same toggle samesite lax max_age 604800 path "/api/auth", return {"message":"logged in"}.
- POST /refresh: reads request.cookies.get("watchbug_refresh"), if missing 401 not authenticated, try jwt.decode with algorithms ["HS256"], check payload type == refresh else 401 invalid token, lookup user, create new access token, set_cookie watchbug_access same as login, return message.
- POST /logout: sets both cookies to empty value with max_age 0 (deletion via Set-Cookie Max-Age 0), watchbug_access path / and watchbug_refresh path /api/auth, per D-04 no server denylist, return {"message":"logged out"}.
- Update backend/app/main.py to include auth router: from app.routers.auth import router as auth_router, app.include_router(auth_router); update lifespan to call seed_admin(db) and seed_default_project(db): need to create db session inside lifespan because engine exists; implement as async with async_sessionmaker(engine, expire_on_commit=False)() as session: await seed_admin(session, settings.ADMIN_EMAIL, settings.ADMIN_PASSWORD) + seed project, commit; handle case where DB not yet migrated gracefully.
- Gate Secure: use settings.ENV == "production" or not settings.DEBUG as secure flag; ensure .env.example documents ENV=development sets Secure False per Pitfall 5.
- Wire protected routes: update routers/incidents.py GET and PATCH to require Depends(get_current_user) (POST stays public per D-05); verify split in main still allows POST without JWT.
- Docs gate remains; auth endpoints are public.
- Never set Authorization header handling; cookie names must be watchbug_access/watchbug_refresh per Agent Discretion.
</action>
  <verify>
    <automated>pytest backend/tests/test_auth.py -k "login or refresh or logout or protected" -xvs 2>&1 | tail -60</automated>
  </verify>
  <done>Login sets both HttpOnly cookies with correct attributes toggled by ENV, refresh reissues access, logout clears via Max-Age 0, invalid creds 401, protected GET/PATCH enforce JWT cookie via get_current_user, POST ingest remains public.</done>
</task>

<task type="auto">
  <name>Auth tests and SEC-05 hardening (.env.example, no secrets)</name>
  <files>backend/tests/test_auth.py, backend/tests/conftest.py, .env.example</files>
  <read_first>02-RESEARCH.md Validation Architecture Phase Requirements→Test Map for AUTH, Security Domain V2/V3/V14, 02-PATTERNS.md tests/test_*.py table, 02-CONTEXT.md D-03 seeded admin specifics idempotent update, documentation/mentorship-pack.md SEC-06</read_first>
  <acceptance_criteria>pytest backend/tests/test_auth.py covers 5 cases: test_password_is_hashed (hash != plaintext, verify true), test_login_sets_cookies (200 + Set-Cookie HttpOnly Lax watchbug_access), test_login_invalid_401, test_protected_401 (GET incidents without cookie 401), test_protected_with_cookie_ok (login then GET succeeds), test_logout_clears (logout sets Max-Age 0 and subsequent GET fails 401), test_refresh_flow (login then refresh reissues access); .env.example exists with DATABASE_URL JWT_SECRET ADMIN_EMAIL ADMIN_PASSWORD CORS_ORIGINS DOCS_ENABLED etc documented; grep -R JWT_SECRET backend/app shows only Field definitions not hardcoded values; grep -R ADMIN_PASSWORD backend/app shows only Field/env usage</acceptance_criteria>
  <action>Per SEC-05 zero secrets in code:
- Create/complete backend/tests/test_auth.py with fixtures from conftest (async_client, seeded admin user via seed_admin or direct). Implement tests: test_password_is_hashed inserts user and asserts password_hash starts with $2b$ and != plaintext and bcrypt.checkpw passes; test_login_sets_cookies posts {"email":"admin@watchbug.local","password":"from .env"} asserts 200 and response.cookies contains watchbug_access httponly via headers check Set-Cookie contains HttpOnly and SameSite; test_login_invalid_401 posts wrong password asserts 401 detail invalid credentials; test_protected_401 GET /api/incidents without cookie asserts 401 not authenticated; test_protected_with_cookie_ok login then use cookies to GET /api/incidents asserts 200 with paginated shape; test_logout_clears POST /api/auth/logout with cookies asserts Set-Cookie Max-Age=0 and subsequent GET 401; test_refresh_flow POST /api/auth/refresh with refresh cookie asserts new access cookie; test_token_expired via manually crafted expired jwt token also 401 token expired.
- Update backend/tests/conftest.py to add auth helper: async def login(client, email, password) -> dict cookies; seed admin in fixture if needed.
- Update .env.example to include all Settings fields with descriptive comments: DATABASE_URL postgresql+asyncpg://..., JWT_SECRET with note generate via python -c "import secrets; print(secrets.token_urlsafe(32))" min_length 32, JWT_ALGORITHM HS256, ACCESS_TOKEN_EXPIRE_MINUTES 60, REFRESH_TOKEN_EXPIRE_DAYS 7, ADMIN_EMAIL, ADMIN_PASSWORD, CORS_ORIGINS comma-separated, DOCS_ENABLED bool, MAX_PAYLOAD_BYTES 102400, PROJECT_API_KEY/dev key, ENV development/production toggle. Ensure every field in backend/app/config.py appears.
- Run grep checks: `grep -R "JWT_SECRET.*="` backend/app --include="*.py"` must not show hardcoded literal secret; only Field definitions. Same for ADMIN_PASSWORD. Commit with no .env containing secrets (backend/.env gitignored).
</action>
  <verify>
    <automated>pytest backend/tests/test_auth.py -v 2>&1 | tail -40; echo "---"; test -f .env.example && echo ".env.example exists" || echo "missing"; grep -R "JWT_SECRET" backend/app --include="*.py" 2>&1 | cat; echo "grep done"</automated>
  </verify>
  <done>All auth tests green with real PG, .env.example commits documenting all env vars, no secrets hardcoded, bcrypt hashes verified, JWT cookie flow complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client->POST /api/auth/login | Untrusted credentials, brute-force attempt surface |
| cookie->GET/PATCH /api/incidents | JWT cookie is bearer token; theft via XSS would be critical |
| env -> Settings | Secrets entry point must not be logged |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | ASVS |
|-----------|----------|-----------|----------|-------------|-----------------|------|
| T-02-02-01 | Spoofing | POST /api/auth/login brute force | high | mitigate | bcrypt cost12 slows guesses + slowapi 60/min per IP on auth routes (wired in Plan03 but doc here); generic 401 invalid credentials no user enumeration | V2 |
| T-02-02-02 | Info disclosure | JWT theft via XSS | high | mitigate | HttpOnly cookies (D-01) JS cannot read, SameSite Lax blocks cross-site POST, Secure flag true in prod per Pitfall 5 toggle | V3 |
| T-02-02-03 | Tampering | JWT algorithm confusion | high | mitigate | PyJWT decode with algorithms=["HS256"] allowlist only; never jose; HS256 chosen per discretion for single-instance self-hosted | V6 |
| T-02-02-04 | Spoofing | CSRF on PATCH status | medium | mitigate | SameSite Lax on auth cookies; ingest uses credentials omit so CSRF irrelevant; PATCH requires cookie + SameSite | V3 |
| T-02-02-05 | Info disclosure | credential leakage via errors | medium | mitigate | Generic detail invalid credentials, never echo password or hash, IntegrityError mapped generic | V7 |
| T-02-02-06 | Tampering | seeded admin open registration | low | accept | No registration endpoint, users table canonical only seeded via .env startup; rationale low because self-hosted single admin per D-03 one-way | V4 |
| T-02-02-SC | Tampering | pip installs | high | mitigate | Verified stack, no postinstalls, PyJWT bcrypt legitimacy per RESEARCH audit | V14 |
</threat_model>

<verification>
`pytest backend/tests/test_auth.py -v` 7+ tests green; `curl -i -X POST http://localhost:8000/api/auth/login -H "Content-Type: application/json" -d '{"email":"admin@watchbug.local","password":"admin123"}'` returns Set-Cookie watchbug_access HttpOnly SameSite=Lax; `curl -i http://localhost:8000/api/incidents` without cookie returns 401; with cookie `curl -b "watchbug_access=..." http://localhost:8000/api/incidents` returns 200; `curl -i -X POST http://localhost:8000/api/auth/logout -b "watchbug_access=...; watchbug_refresh=..."` returns Set-Cookie Max-Age=0.
</verification>

<success_criteria>
- Login with seeded ADMIN_EMAIL/ADMIN_PASSWORD returns 200 and both HttpOnly cookies with correct Max-Age and SameSite Lax and Secure toggle
- Wrong password returns 401 invalid credentials without leaking existence
- Refresh with valid refresh cookie reissues access cookie; logout clears both via Max-Age 0
- GET/PATCH incidents without cookie return 401; with valid cookie return 200 (not 500)
- Passwords stored as bcrypt hash cost12, seed_admin idempotent and rotates on env change
- JWT uses PyJWT HS256 with jti/sub/exp/iat, decode with algorithm allowlist
- .env.example documents all vars, no secret hardcoded
</success_criteria>

<output>
Create .planning/phases/02-backend-api/02-02-SUMMARY.md when done
</output>

## Artifacts this phase produces
- backend/app/services/auth_service.py — hash_password, verify_password bcrypt direct gensalt12, create_access_token/create_refresh_token jwt.encode HS256 jti/sub/exp/iat, verify_token, seed_admin upsert [creates]
- backend/app/dependencies.py — get_current_user reads watchbug_access cookie jwt.decode HS256 lookup User, raises 401 variants [creates]
- backend/app/routers/auth.py — APIRouter /api/auth login refresh logout with HttpOnly SameSite Lax Secure toggle Max-Age cookies [creates]
- backend/app/main.py — updated to include auth router and lifespan seed_admin+seed_default_project [modifies]
- backend/app/config.py — updated with ENV/DEBUG toggle for Secure flag if missing [modifies]
- backend/tests/conftest.py — updated with login helper and seeded admin fixture [modifies]
- backend/tests/test_auth.py — 7 tests for hashed storage, login cookies, invalid 401, protected 401/ok, logout, refresh, expired [creates]
- .env.example — finalized with all Settings fields documented (DATABASE_URL, JWT_SECRET generation note, ADMIN_*, CORS_ORIGINS, DOCS_ENABLED, MAX_PAYLOAD_BYTES, etc) [modifies]
