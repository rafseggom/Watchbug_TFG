---
phase: 02-backend-api
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/versions/001_initial.py
  - backend/app/__init__.py
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/db.py
  - backend/app/models/__init__.py
  - backend/app/models/incident.py
  - backend/app/models/user.py
  - backend/app/models/project.py
  - backend/app/schemas/__init__.py
  - backend/app/schemas/incident.py
  - backend/app/schemas/auth.py
  - backend/app/schemas/common.py
  - backend/app/services/incident_service.py
  - backend/app/services/project_service.py
  - backend/app/routers/__init__.py
  - backend/app/routers/incidents.py
  - backend/app/routers/health.py
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_health.py
  - backend/tests/test_incidents_ingest.py
  - .env.example
  - backend/.env
autonomous: true
requirements:
  - API-01
  - API-02
  - API-05
  - DB-01
  - DB-02
  - DB-03
  - DB-04
  - SEC-05
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - SDK can POST incident payload to /api/incidents and receive 201 with {id, status: Pending, created_at}; row exists in PostgreSQL with type Bug/Feedback, BYTEA screenshot, JSONB payload, project_id FK
    - GET /api/health is public and returns {status: ok, db: connected|disconnected} with real SELECT 1 probe per API-05
    - FastAPI app uses lifespan asynccontextmanager (not on_event), Pydantic Settings from .env, AsyncEngine with expire_on_commit=False, Alembic async env.py; .env.example documents every setting
    - Incidents/users/projects tables exist with correct columns (incidents: id UUID PK, type, status, payload JSONB, screenshot BYTEA LargeBinary, project_id FK, created_at/updated_at; users: id email unique password_hash; projects: id name api_key unique)
    - Alembic upgrade head has been executed (blocking) and is idempotent; seeded default project and admin placeholder work
  artifacts:
    - backend/pyproject.toml
    - backend/app/main.py
    - backend/app/config.py
    - backend/app/db.py
    - backend/app/models/incident.py
    - backend/app/models/user.py
    - backend/app/models/project.py
    - backend/alembic/env.py
    - backend/alembic/versions/001_initial.py
    - backend/app/routers/health.py
    - backend/app/routers/incidents.py
    - backend/app/schemas/incident.py
    - backend/tests/conftest.py
    - .env.example
  key_links:
    - SDK transport X-Watchbug-Key header -> project_service lookup -> project_id FK on Incident
    - Lifespan asynccontextmanager -> create engine -> Alembic upgrade -> seed -> yield -> dispose; startup failure aborts app
    - POST /api/incidents raw body -> Pydantic IncidentCreate validation (TRN-04) -> Base64 decode -> BYTEA -> sanitize hook -> commit -> refresh -> 201
    - GET /api/health -> Depends(get_db) -> SELECT 1 -> db connected/disconnected
<!-- planner-discipline-allow: asynccontextmanager -->
<!-- planner-discipline-allow: on_event -->
<!-- planner-discipline-allow: expire_on_commit=False -->
---

<objective>
Tracer vertical slice: scaffold the FastAPI backend from zero to a working end-to-end ingest path that proves DB persistence. Delivers project manifest, Settings, async DB engine, all 3 tables + Alembic, health probe with lifespan, and minimal POST /api/incidents that accepts SDK's exact contract (X-Watchbug-Key, JSON body, Bug vs Feedback validation, BYTEA decode) and persists to PostgreSQL.

Purpose: Prove architecture end-to-end (lifespan + async DB + BYTEA + migration + routing) before layering auth and hardening. A failing tracer aborts early instead of after 10 committed layers.
Output: Runnable backend where `POST /api/incidents` returns 201 and `GET /api/health` returns DB status, with migration applied and tests green against real PostgreSQL.
</objective>

<execution_context>
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/workflows/execute-plan.md
@E:/Proyectos Github/Watchbug_TFG/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-api/02-CONTEXT.md
@.planning/phases/02-backend-api/02-RESEARCH.md
@.planning/phases/02-backend-api/02-PATTERNS.md
@sdk/src/transport/sender.ts
@sdk/src/transport/validation.ts
</context>

<tasks>

<task type="tracer" reversibility="one-way">
  <name>End-to-end tracer: scaffold + lifespan + DB + health + minimal ingest</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/db.py, backend/app/models/__init__.py, backend/app/models/incident.py, backend/app/models/user.py, backend/app/models/project.py, backend/app/schemas/__init__.py, backend/app/schemas/incident.py, backend/app/schemas/auth.py, backend/app/schemas/common.py, backend/app/services/incident_service.py, backend/app/services/project_service.py, backend/app/routers/__init__.py, backend/app/routers/incidents.py, backend/app/routers/health.py, backend/app/main.py, backend/app/__init__.py, .env.example, backend/.env</files>
  <read_first>02-CONTEXT.md (D-05/D-11/Agent Discretion), 02-RESEARCH.md Patterns 1-4/11-13 + Anti-Patterns, 02-PATTERNS.md all pattern assignments, sdk/src/transport/sender.ts (lines 24-35 credentials omit + X-Watchbug-Key), sdk/src/transport/validation.ts (lines 16-73 TRN-04), .planning/research/STACK.md locked stack</read_first>
  <acceptance_criteria>POST /api/incidents with project key header, Bug type with screenshot Base64 PNG + metadata url/userAgent/timestamp + consoleLogs array returns 201 {id: uuid, status: Pending, created_at: iso8601}; row exists in PostgreSQL incidents table with payload JSONB (sanitized), screenshot BYTEA (bytes), project_id FK matching seeded project; Feedback without consoleLogs also returns 201; Bug without consoleLogs returns 422 with loc ["body","consoleLogs"]; GET /api/health returns {status: ok, db: connected} when DB up</acceptance_criteria>
  <action>Per D-11 (reversibility: one-way BYTEA decision) and D-05 (one-way public ingest with PROJECT_KEY):
- Create backend/pyproject.toml with [project] name watchbug-backend, requires-python >=3.10, deps: fastapi[standard]==0.141.1, pydantic==2.13.5, pydantic-settings==2.15.0, sqlalchemy[asyncio]==2.0.52, asyncpg==0.31.0, alembic==1.19.1, pyjwt==2.13.0, bcrypt==5.0.0, slowapi==0.1.10, httpx, plus dev pytest pytest-asyncio ruff; set [tool.pytest.ini_options] asyncio_mode=auto, [tool.ruff] config.
- Create backend/app/config.py: class Settings(BaseSettings) with SettingsConfigDict(env_file=".env", env_file_encoding="utf-8", extra="ignore"), fields DATABASE_URL default postgresql+asyncpg://watchbug:watchbug@localhost:5432/watchbug, JWT_SECRET min_length 32, JWT_ALGORITHM HS256 default, ACCESS_TOKEN_EXPIRE_MINUTES 60, REFRESH_TOKEN_EXPIRE_DAYS 7, ADMIN_EMAIL default admin@watchbug.local, ADMIN_PASSWORD min_length 8, CORS_ORIGINS default http://localhost:5173, DOCS_ENABLED bool default False, MAX_PAYLOAD_BYTES 102400 int; computed_field cors_origins_list splits CORS_ORIGINS; get_settings() cached with lru_cache. Never use os.getenv.
- Create backend/app/db.py: AsyncEngine via create_async_engine(DATABASE_URL, echo=False), async_sessionmaker with expire_on_commit=False (CRITICAL per Pitfall 2), DeclarativeBase class Base, get_db dependency yielding AsyncSession per request. DATABASE_URL must be postgresql+asyncpg:// not postgresql://.
- Create models per RESEARCH Pattern 3+13+D-11: incident.py Incident(id UUID PK default uuid4, type String16, status String16 default Pending, payload JSONB, screenshot LargeBinary nullable False, project_id UUID FK projects.id, created_at/updated_at DateTime timezone server_default func.now/onupdate), user.py User(id UUID PK, email String255 unique indexed, password_hash String60, created_at), project.py Project(id UUID PK, name String255, api_key String64 unique indexed, created_at). Use UUID(as_uuid=True) + mapped_column. Ensure backend/app/models/__init__.py re-exports Base + Incident+User+Project for Alembic autogenerate (Pitfall 10).
- Create schemas per RESEARCH Code Example + PATTERNS: schemas/incident.py ConsoleEntry(level Literal, args, timestamp), IncidentCreate(type Literal Bug|Feedback plus lowercase bug|feedback accepted, screenshot str min_length 1, metadata dict, consoleLogs list[ConsoleEntry]|None, errors list[str] default, notes str|None) with ConfigDict(populate_by_name True), field_validator type mode before normalizing {"bug":"Bug","feedback":"Feedback"} title mapping, field_validator consoleLogs mode after checking Bug requires non-empty else ValueError, field_validator metadata mode after requiring url/userAgent/timestamp. schemas/auth.py LoginRequest(email EmailStr, password str), MessageResponse, schemas/common.py PaginatedResponse items/total/page/size/pages.
- Create services/incident_service.py stubs: sanitize hook (call sanitize_payload if utils exists else identity), decode_screenshot(base64 with data: prefix strip, base64.b64decode validate True -> binascii.Error mapped to 422), create_incident(db, data, project_id) strips screenshot from JSONB before storage per Open Question Q2, does sanitize_payload on model_dump, decodes BYTEA, creates Incident type/status Pending/payload/screenshot/project_id, add commit refresh. services/project_service.py resolve_project(request, db) reads x-watchbug-key OR x-project-key (Starlette normalizes), queries select Project where api_key==key, 401 invalid project key if missing/not found (D-08), seed_default_project for startup.
- Create routers/health.py public GET /api/health with try db.execute(text("SELECT 1")) -> db connected else disconnected, returns {status: ok, db}. Create routers/incidents.py minimal: POST /api/incidents status 201 public (NO JWT dependency per D-05) -> resolve project, read raw body later but for tracer do Pydantic parse via IncidentCreate, handle payload size per-route later in hardening but include basic check; on 401/422/201 correctness per D-06/D-07. GET /api/incidents and PATCH stubs returning 401 via placeholder is ok for tracer but router must be mountable.
- Create backend/app/main.py with lifespan asynccontextmanager: startup seeds admin later (placeholder call) but tracer must run Alembic or create_all; for greenfield use lifespan that imports config get_settings, calls await seed placeholder, yields, disposes engine. Set docs_url=None/openapi_url=None/redoc_url=None base, docs gated later. Middleware order: later add payload size/CORS/limiter but tracer mounts minimal CORS allowlist for testing.
- Create .env.example documenting every Settings field with example values and comments (SEC-05/DEP-04), and backend/.env actual file for local dev with generated JWT_SECRET (secrets.token_urlsafe 32) and ADMIN_EMAIL/ADMIN_PASSWORD.
- Do NOT use deprecated patterns: no @validator/class Config, no on_event, no passlib, no python-jose, no psycopg2, no TEXT for screenshot.
Implement verbatim the RESEARCH patterns for lifespan (Pattern1), Settings (Pattern2), AsyncEngine expire_on_commit False (Pattern3), BYTEA LargeBinary (Pattern13), project key dual header (Pattern11). Trace is production-quality not prototype.</action>
  <verify>
    <automated>pip install -q -r backend/requirements.txt 2>&1 || pip install -q "fastapi[standard]==0.141.1" "pydantic==2.13.5" "pydantic-settings==2.15.0" "sqlalchemy[asyncio]==2.0.52" "asyncpg==0.31.0" "alembic==1.19.1" "pyjwt==2.13.0" "bcrypt==5.0.0" "slowapi==0.1.10" httpx pytest pytest-asyncio email-validator 2>&1 | tail -5; python -c "import fastapi, pydantic, sqlalchemy, alembic, jwt, bcrypt, slowapi; print('deps ok')"</automated>
  </verify>
  <done>Tracer files exist, FastAPI app imports without error, lifespan is asynccontextmanager not on_event, Settings loads from .env, AsyncEngine expire_on_commit=False verified by grep, models importable and Base.metadata contains 3 tables.</done>
</task>

<task type="auto" reversibility="one-way">
  <name>Alembic async setup and blocking schema push + seed</name>
  <files>backend/alembic.ini, backend/alembic/env.py, backend/alembic/versions/001_initial.py</files>
  <read_first>02-RESEARCH.md Pattern 4 Alembic Async Migrations (lines 411-438) + Pitfall 10 empty migration, 02-PATTERNS.md alembic/env.py assignment, backend/app/models/__init__.py, backend/app/config.py</read_first>
  <acceptance_criteria>alembic revision --autogenerate produces 001_initial.py with op.create_table for incidents/users/projects (not empty upgrade pass); `alembic upgrade head` creates tables (verified SELECT to_regclass); downgrade then upgrade cycle is idempotent; seeded default project row exists with api_key matching env PROJECT_API_KEY or generated; verify `alembic current` shows head</acceptance_criteria>
  <action>Per D-11 one-way BYTEA and DB-04:
- Initialize alembic.ini with sqlalchemy.url overridden at runtime by env.py (set placeholder postgresql+asyncpg://watchbug:watchbug@localhost:5432/watchbug but env.py overrides via config.set_main_option("sqlalchemy.url", get_settings().DATABASE_URL)).
- Create alembic/env.py async variant verbatim from RESEARCH Code Example: import asyncio, fileConfig, async_engine_from_config, pool NullPool, context, from app.models import Base (must import all models transitively), from app.config import get_settings, set target_metadata = Base.metadata, define do_run_migrations(connection) with context.configure + begin_transaction + run_migrations, async run_migrations_online() with async_engine_from_config + run_sync(do_run_migrations) + dispose, dispatch offline vs online with asyncio.run.
- Critical: backend/app/models/__init__.py must import Incident/User/Project before env import, otherwise autogenerate empty (Pitfall 10). Verify by running `alembic revision --autogenerate -m "initial"` and checking upgrade contains op.create_table.
- Create alembic/versions/001_initial.py via autogenerate; ensure columns match spec: incidents.id UUID primary, type String(16), status String(16) default Pending, payload JSONB, screenshot LargeBinary BYTEA, project_id UUID FK, created_at/updated_at timezone; users id email unique password_hash; projects id name api_key unique. If Enums are used, ensure PostgreSQL ENUM or String check - keep String for simplicity unless research demands PG Enum.
- [BLOCKING] Run database schema push: ensure PostgreSQL is reachable (docker run -d --name watchbug-pg -e POSTGRES_USER=watchbug -e POSTGRES_PASSWORD=watchbug -e POSTGRES_DB=watchbug -p 5432:5432 postgres:16-alpine if not running; verify with pg_isready or SELECT 1), then execute `alembic upgrade head` (or `uv run alembic upgrade head` / `python -m alembic upgrade head` fallback) with DATABASE_URL set. This MUST run after schema files are complete and before verification; build/type checks pass without push creating false-positive. Document non-TTY workaround: set DATABASE_URL env, no prompts.
- Implement lifespan seeding hook: create services/auth_service seed_admin and services/project_service seed_default_project called from lifespan startup idempotently (if not exists insert, else update password_hash if ADMIN_PASSWORD changed). For now implement minimal seed that does not break tracer if bcrypt not yet wired in Plan02; default project seeded with api_key from DEFAULT_PROJECT_API_KEY env or deterministic dev key "wb_test_project_key_123".
- Verify tables exist via `SELECT to_regclass('public.incidents')` and row count queries; verify `alembic current` outputs head.
Do not use sync psycopg2 driver; keep postgresql+asyncpg:// everywhere.</action>
  <verify>
    <automated>python -c "from app.models import Base; print(list(Base.metadata.tables.keys()))" 2>&1; alembic -c backend/alembic.ini current 2>&1 | tail -3; alembic -c backend/alembic.ini upgrade head 2>&1 | tail -5; python -c "import asyncio; from sqlalchemy import text; from sqlalchemy.ext.asyncio import create_async_engine; from app.config import get_settings; s=get_settings(); print('DB', s.DATABASE_URL.split('@')[-1]); e=create_async_engine(s.DATABASE_URL); asyncio.run(e.dispose()); print('engine ok')"</automated>
  </verify>
  <done>Alembic env.py async, 001_initial creates 3 tables with correct columns (UUID, JSONB, BYTEA), upgrade head executed and verified via SELECT 1, head current, seed project row exists.</done>
</task>

<task type="auto">
  <name>Test harness and tracer verification</name>
  <files>backend/tests/conftest.py, backend/tests/test_health.py, backend/tests/test_incidents_ingest.py, backend/tests/__init__.py</files>
  <read_first>02-RESEARCH.md Validation Architecture Test Map + Pattern 12/13, 02-PATTERNS.md tests/conftest.py assignment, 02-CONTEXT.md D-05..D-08/D-11, backend/app/main.py, backend/app/routers/incidents.py</read_first>
  <acceptance_criteria>pytest -q passes with real PostgreSQL: test_health_db_connected (GET /api/health -> {status:ok, db:connected}), test_health_db_disconnected (mocked failure), test_create_bug_success (POST Bug with screenshot small red dot Base64 + metadata + consoleLogs -> 201 {id, status Pending, created_at} and DB row with BYTEA and payload JSONB without screenshot), test_feedback_without_logs_ok (Feedback no consoleLogs -> 201), test_bug_without_logs_422 (Bug missing consoleLogs -> 422 loc ["body","consoleLogs"]), test_invalid_project_key_401 (bad key -> 401 invalid project key)</acceptance_criteria>
  <action>Per RESEARCH Validation Architecture Wave 0 and PATTERNS tests/conftest:
- Create backend/tests/conftest.py with fixtures: event loop asyncio, engine fixture scope session using get_settings().DATABASE_URL with NullPool for test isolation, db_session yielding AsyncSession with Base.metadata.create_all/drop handling via transaction rollback or create_all per test function, async_client using httpx.AsyncClient with ASGITransport(app=app) base_url http://test, override get_db dependency to use test session, seeded_project fixture inserting Project(name="test-project", api_key="test-project-key-123") idempotently, helpers valid_screenshot() returning minimal 1x1 PNG Base64 (iVBORw0KG...) without data: prefix and with data URL for both cases, valid_metadata() dict with url/userAgent/timestamp, and auth cookie helper stub (no JWT yet).
- Create backend/tests/test_health.py: test_health_ok calls GET /api/health asserts 200 and json status ok db connected, test_health_db_disconnected monkeypatches get_db to raise or override engine to fail.
- Create backend/tests/test_incidents_ingest.py: implement RESEARCH Test Map cases: test_create_bug_success posts type Bug lower and title case both should succeed after normalization, screenshot stripped data URL prefix, consoleLogs list, asserts 201 and response id is uuid and status Pending and created_at iso8601, then verify via db query incident exists payload has type Bug status Pending and screenshot is bytes length >0; test_feedback_without_logs_ok posts type Feedback without consoleLogs -> 201; test_bug_without_logs_422 posts Bug without consoleLogs -> 422 and detail loc contains body consoleLogs; test_invalid_project_key_401 posts with X-Watchbug-Key bad -> 401 detail invalid project key and also test missing header ->401; also test missing X-Project-Key fallback header alias.
- Ensure conftest overrides DATABASE_URL from env or TEST_DATABASE_URL, falls back to sqlite+aiosqlite only if explicitly set but integration tests MUST use real PG for BYTEA/JSONB correctness per RESEARCH Assumptions A4. Document that local dev needs docker postgres:16-alpine run command.
- Run pytest -q and ensure <30s, no watch mode flags, asyncio_mode auto.
Do not mock BYTEA; use real bytes via base64.b64decode validate True.</action>
  <verify>
    <automated>pytest backend/tests/test_health.py::test_health_ok -xvs 2>&1 | tail -30; pytest backend/tests/test_incidents_ingest.py -k "test_create_bug_success or test_bug_without_logs_422 or test_invalid_project_key_401" -xvs 2>&1 | tail -50</automated>
  </verify>
  <done>conftest fixtures working with real PG, health returns ok/connected, ingest POST 201 for Bug/Feedback valid, 422 for Bug without logs with loc body.consoleLogs, 401 for invalid project key, BYTEA stored and retrievable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client->POST /api/incidents | Untrusted SDK/attacker JSON, arbitrary Origin, must validate project key, size, schema before DB |
| DB engine -> PostgreSQL | Trusted but must not leak credentials in errors |
| alembic migration -> DB | Supply-chain for schema; must not be empty |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan | ASVS |
|-----------|----------|-----------|----------|-------------|-----------------|------|
| T-02-01-01 | Tampering | POST /api/incidents schema validation | high | mitigate | Pydantic IncidentCreate with field_validator type normalize + consoleLogs required for Bug (TRN-04) returns 422 with loc detail; use ConfigDict not legacy validator | V5 |
| T-02-01-02 | Tampering | screenshot BYTEA decode | medium | mitigate | base64.b64decode validate=True, strip data: prefix, binascii.Error -> 422 invalid screenshot encoding; store LargeBinary not TEXT | V12 |
| T-02-01-03 | Spoofing | project key auth on ingest | high | mitigate | resolve_project checks X-Watchbug-Key primary fallback X-Project-Key against projects.api_key DB lookup, missing/invalid -> 401 invalid project key per D-08 | V4 |
| T-02-01-04 | Information disclosure | error messages leak schema or secrets | medium | mitigate | Generic detail messages only, never echo DATABASE_URL/JWT_SECRET/stack trace; IntegrityError mapped to 400/409 | V7 |
| T-02-01-SC | Tampering | npm/pip package installs | high | mitigate | Package legitimacy gate from RESEARCH Package Legitimacy Audit - all deps verified via pip registry, no slop; no postinstall scripts | V14 |
</threat_model>

<verification>
Tracer gate: `pytest backend/tests/test_health.py backend/tests/test_incidents_ingest.py -q` green against real PG; `curl http://localhost:8000/api/health` returns status ok; `curl -X POST http://localhost:8000/api/incidents -H "X-Watchbug-Key: test-project-key-123" -H "Content-Type: application/json" -d '{"type":"Feedback","screenshot":"iVBORw0KG...","metadata":{"url":"https://example.com","userAgent":"test","timestamp":"2026-08-31T00:00:00Z"},"errors":[]}'` returns 201 with id/status Pending.
Schema push gate: `alembic -c backend/alembic.ini current` shows head, `SELECT to_regclass('public.incidents')` returns incidents.
Lifespan gate: grep -c "asynccontextmanager" backend/app/main.py >=1 and grep -c "on_event" backend/app/main.py ==0; grep -c "expire_on_commit=False" backend/app/db.py >=1.
</verification>

<success_criteria>
- POST /api/incidents with valid Bug (consoleLogs required) and Feedback (no logs) both return 201 and persist to PG with BYTEA screenshot and JSONB payload sans screenshot
- Bug without consoleLogs returns 422 with loc ["body","consoleLogs"] (Pydantic v2 field_validator fires)
- Missing/invalid X-Watchbug-Key or X-Project-Key returns 401 {detail: invalid project key}
- GET /api/health returns {status: ok, db: connected} public, and disconnected when DB down
- Lifespan is asynccontextmanager, Settings from .env, AsyncEngine expire_on_commit=False, Alembic async env imports Base, 001_initial creates 3 tables, upgrade head executed blocking before verify
- .env.example documents all settings; no secrets hardcoded (grep JWT_SECRET in backend/app shows only Field definition, not value)
</success_criteria>

<output>
Create .planning/phases/02-backend-api/02-01-SUMMARY.md when done
</output>

## Artifacts this phase produces
- backend/pyproject.toml — project manifest, deps, tool configs (ruff, pytest asyncio_mode auto) [creates]
- backend/alembic.ini — alembic config url overridden by env.py [creates]
- backend/alembic/env.py — async engine env with Base.metadata, do_run_migrations, asyncio.run [creates]
- backend/alembic/versions/001_initial.py — initial migration op.create_table incidents/users/projects [creates]
- backend/app/__init__.py — package marker [creates]
- backend/app/main.py — FastAPI app with lifespan asynccontextmanager, docs gated, router includes [creates]
- backend/app/config.py — Settings(BaseSettings) with SettingsConfigDict, computed cors_origins_list, get_settings lru_cache [creates]
- backend/app/db.py — Base(DeclarativeBase), create_async_engine postgresql+asyncpg://, async_sessionmaker expire_on_commit False, get_db dependency [creates]
- backend/app/models/__init__.py — re-exports Base+Incident+User+Project for autogenerate [creates]
- backend/app/models/incident.py — Incident table UUID PK, type/status String, payload JSONB, screenshot LargeBinary BYTEA, project_id FK, timestamps [creates]
- backend/app/models/user.py — User table UUID PK, email unique indexed, password_hash String60, created_at [creates]
- backend/app/models/project.py — Project table UUID PK, name, api_key unique indexed, created_at [creates]
- backend/app/schemas/__init__.py — barrel [creates]
- backend/app/schemas/incident.py — ConsoleEntry + IncidentCreate with field_validator type normalize + consoleLogs TRN-04 + metadata check, IncidentOut [creates]
- backend/app/schemas/auth.py — LoginRequest EmailStr + MessageResponse [creates]
- backend/app/schemas/common.py — PaginationParams, PaginatedResponse [creates]
- backend/app/services/incident_service.py — sanitize hook, decode_screenshot validate, create_incident commit refresh [creates]
- backend/app/services/project_service.py — resolve_project dual header + seed_default_project [creates]
- backend/app/routers/__init__.py — [creates]
- backend/app/routers/incidents.py — POST /api/incidents public 201/401/422 minimal (auth added later) [creates]
- backend/app/routers/health.py — GET /api/health SELECT 1 probe public [creates]
- backend/tests/__init__.py, backend/tests/conftest.py — fixtures async_client/db_session/seeded_project [creates]
- backend/tests/test_health.py, backend/tests/test_incidents_ingest.py — tracer tests per Validation Map [creates]
- .env.example — documents DATABASE_URL JWT_SECRET ADMIN_EMAIL ADMIN_PASSWORD CORS_ORIGINS DOCS_ENABLED etc [creates]
- backend/.env — dev env with secrets (not committed, gitignored) [creates]
