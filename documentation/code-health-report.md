# Code Health Audit Report

**Date:** 2026-09-09  
**Workspace:** Watchbug TFG  
**Mode:** Full repository scan (Mode B)

---

## Summary

| Bucket | Count | Items |
|--------|-------|-------|
| Safe to delete | 0 | — |
| Consolidation candidate | 0 | — |
| Looks bad but is fine | 5 | False positives (test-only reachable) |
| Needs runtime verification | 2 | Potentially dead exports |

---

## 1. Safe to Delete

**None.** All flagged items have explanations or require further verification.

---

## 2. Consolidation Candidates

**None.** No duplicate function names detected across files.

---

## 3. Looks Bad But Is Fine (False Positives)

These exports are flagged by Knip but are actually needed for tests.

### SDK Workspace

| File | Export | Reason |
|------|--------|--------|
| `src/capture/console.ts:7` | `SECRET_PATTERNS` | Used in `tests/unit/console.test.ts` |
| `src/capture/console.ts:17` | `redactSecrets` | Used in `tests/unit/console.test.ts` |
| `src/transport/draft.ts:30` | `loadDraft` | Used in `tests/unit/draft.test.ts` |
| `src/widget/i18n.ts:5` | `TRANSLATIONS` | Used in `tests/unit/i18n.test.ts` |

**Verdict:** These are test-only reachable code. The exports are needed for test imports but aren't used in production. This is expected behavior for testable code.

### Panel Workspace

| File | Export | Reason |
|------|--------|--------|
| `src/router.ts:8` | `navigate`, `parseHash` | Re-exported from `utils/hash.ts`, used internally by router |

**Verdict:** These are re-exports for API consistency. The functions are used internally by the router module.

---

## 4. Needs Runtime Verification

These exports are flagged and may be dead code. Manual verification required.

### Panel Workspace

| File | Export | Line | Analysis |
|------|--------|------|----------|
| `src/api.ts:30` | `rawFetch` | 30 | Exported but never called anywhere in codebase or tests |
| `src/auth.ts:55` | `clearRefresh` | 55 | Exported but never called anywhere in codebase or tests |

**Investigation:**

1. **`rawFetch`** - This is the base fetch wrapper that `apiFetch` uses internally. However, `rawFetch` itself is never imported or called directly by any other module. The `apiFetch` function in the same file calls it internally, but the export is unused.

   **Recommendation:** Remove the `export` keyword. The function can remain as a private helper within `api.ts`.

2. **`clearRefresh`** - This function clears the refresh token from localStorage. It's exported but never called. There's a `logout` function in `auth.ts` that handles cookie clearing, but `clearRefresh` is not invoked.

   **Recommendation:** Verify if this function is needed for future use or manual debugging. If not, remove it.

---

## Dev Dependencies Flagged

| File | Dependency | Reason |
|------|------------|--------|
| `sdk/package.json` | `jsdom` | Test environment dependency |
| `sdk/package.json` | `vitest` | Test framework |

**Verdict:** These are expected dev dependencies for testing. Not dead code.

---

## Files Flagged

| File | Reason |
|------|--------|
| `sdk/vitest.e2e.config.ts` | E2E test configuration, not a source file |

**Verdict:** Configuration file, not dead code.

---

## Suppression Markers

No suppression markers found in the codebase.

---

## Recommendations

### Immediate Actions (Safe)
1. Remove `export` from `rawFetch` in `panel/src/api.ts:30` (make it private)
2. Review `clearRefresh` in `panel/src/auth.ts:55` - consider removing if not needed

### Future Improvements
1. Add `// audit:code-health-ok — <reason>` markers to test-only exports
2. Consider adding `__mocks__` and `fixtures` directories to knip.json ignore list
3. Run this audit periodically before major cleanup PRs

---

## Raw Knip Output

<details>
<summary>SDK Comprehensive Mode</summary>

```json
{
  "exports": [
    {"file": "src/capture/console.ts", "name": "SECRET_PATTERNS", "line": 7},
    {"file": "src/capture/console.ts", "name": "redactSecrets", "line": 17},
    {"file": "src/transport/draft.ts", "name": "loadDraft", "line": 30},
    {"file": "src/widget/i18n.ts", "name": "TRANSLATIONS", "line": 5}
  ]
}
```
</details>

<details>
<summary>Panel Comprehensive Mode</summary>

```json
{
  "exports": [
    {"file": "src/api.ts", "name": "rawFetch", "line": 30},
    {"file": "src/auth.ts", "name": "clearRefresh", "line": 55},
    {"file": "src/router.ts", "name": "navigate", "line": 8},
    {"file": "src/router.ts", "name": "parseHash", "line": 8},
    {"file": "src/router.ts", "name": "onRoute", "line": 46}
  ]
}
```
</details>

---

*Report generated by Code Health Auditor v1.0.0*
